services:
  test:
    build: .
    command: python -m unittest discover -s tests
  live-srt:
    build: .
    environment:
      VIDEOSIM_LIVE_SRT: "1"
    command: python -m unittest tests.test_live_srt
