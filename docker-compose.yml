services:
  test:
    build: .
    command: python -m unittest tests.test_docs_contract tests.test_cli_video_feed
  live-srt:
    build: .
    environment:
      VIDEOSIM_LIVE_SRT: "1"
    command: python -m unittest tests.test_live_srt
