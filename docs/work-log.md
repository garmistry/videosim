# Work Log

## 2026-06-28

- Added dependency installer for Linux and macOS at `scripts/install-deps.sh`.
- Added repository rule to commit successful completions and keep this work log updated.
- Completed Milestone 0 product-contract docs: `README.md`, `TEST_PLAN.md`, `ACCEPTANCE_MATRIX.md`, `TEST_GAPS.md`, `KNOWN_LIMITATIONS.md`, and `RUNBOOK.md`.
- Added `tests/test_docs_contract.py` to enforce critical requirement/test mapping and required feed mode behavior mapping.
- Added Python cache ignores for repeatable local test runs.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Skipped checks: no live SRT, media, GUI, or Docker checks exist yet; those are blocked by later milestone implementation.
- Added minimal `python3 -m videosim start` CLI for a synthetic video-only SRT listener feed using GStreamer.
- Added CLI tests for endpoint generation, GStreamer command construction, invalid port errors, print-command mode, and Ctrl-C stop handling.
- Validation run: `python3 -m unittest tests.test_docs_contract tests.test_cli_video_feed` passed, 9 tests.
- Manual smoke: `python3 -m videosim start --port 9911 --width 320 --height 180 --framerate 10` started, printed `srt://127.0.0.1:9911?mode=caller`, and stopped with Ctrl-C exit code 0.
- Skipped checks: automated receiver video detection and restart proof are still missing, so Milestone 1 remains open.
- Added Docker Linux test harness and live SRT integration test at `tests/test_live_srt.py`.
- Added stop fallback from SIGINT to terminate/kill for stuck media child processes.
- Validation run: `python3 -m unittest tests.test_docs_contract tests.test_cli_video_feed tests.test_live_srt` passed, 12 tests with the live test skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 11 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 1 test proving SRT receiver video detection and restart on the same port.
- Completed Milestone 1 CLI SRT video feed gate at 100% weighted coverage.
- Added generated AAC audio to the default SRT feed with `audiotestsrc` and `--audio-frequency` validation; kept `--no-audio` for video-only operation.
- Updated Docker live SRT test to prove one receiver consumes both H.264 video and AAC audio, then restarts on the same port.
- Validation run: `python3 -m unittest tests.test_docs_contract tests.test_cli_video_feed tests.test_live_srt` passed, 14 tests with the live test skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 13 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 1 test proving audio/video receiver consumption.
- Completed Milestone 2 add-audio gate at 85.7% weighted coverage; skipped P1 30-minute continuity test is recorded in `TEST_GAPS.md`.
- Added generated CEA-608 caption insertion using `fdsrc`, `cccombiner`, and `h264ccinserter`; added `--no-captions`.
- Updated live Docker receiver test to extract captions with `h264ccextractor` and added a no-caption negative check.
- Validation run: `python3 -m unittest tests.test_docs_contract tests.test_cli_video_feed tests.test_live_srt` passed, 17 tests with 2 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 15 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 2 tests proving captions present and absent when disabled.
- Completed Milestone 3 add-closed-captions gate at 85.7% weighted coverage; skipped P1 caption update-over-time verification is recorded in `TEST_GAPS.md`.
- Added flat YAML profile loader, `profiles/srt-normal.yaml`, and `python3 -m videosim start --profile`.
- Added profile tests for valid normal profile loading, CLI command generation, invalid mode failure, missing required fields, schema version checks, and CLI profile error reporting.
- Validation run: `python3 -m unittest discover -s tests` passed, 23 tests with 2 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 23 tests.
- Docker live regression run: `docker compose run --build --rm live-srt` passed, 2 tests.
- Completed Milestone 4 feed-profile gate at 100% weighted coverage.
- Added required static outage profile files for audio-only, video-only, no-captions, black-video, and frozen-video modes.
- Added profile mode presets and pipeline branches for video absence, audio absence, caption absence, black video, and frozen video via `imagefreeze`.
- Validation run: `python3 -m unittest discover -s tests` passed, 25 tests with 2 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 25 tests.
- Docker live regression run: `docker compose run --build --rm live-srt` passed, 2 tests.
- Milestone 5 remains open: live validation for all six static outage profiles, including black-frame and frozen-frame checks, is still pending.
- Added Docker live validation for all six static outage profiles, including track presence/absence, caption presence/absence, black-frame ratio, and near-identical frozen frames.
- Validation run: `python3 -m unittest discover -s tests` passed, 26 tests with 3 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 26 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 3 tests covering normal, no-captions, and all six static outage profiles.
- Completed Milestone 5 static-outage-profile gate at 100% weighted coverage.
- Added `python3 -m videosim validate --profile ...` with human-readable and JSON output.
- Reused validation for live static profiles and added stopped-feed unreachable validation.
- Validation run: `python3 -m unittest discover -s tests` passed, 29 tests with 4 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 29 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 4 tests covering all six modes and stopped feed.
- Completed Milestone 6 automated-validation gate at 100% weighted coverage.
- Added local browser GUI via `python3 -m videosim gui` with start/stop controls, copyable endpoint, status, and logs.
- Added GUI tests for rendered controls/logs and normal-profile feed launch command.
- Added Docker live GUI proof: HTTP start control launches a normal feed and `videosim validate` confirms the GUI-started feed.
- Validation run: `python3 -m unittest discover -s tests` passed, 32 tests with 5 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 32 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 5 tests.
- Completed Milestone 7 minimal-GUI gate at 88.5% weighted coverage.
- Added GUI mode selector for normal, audio-only, video-only, no-captions, black-video, and frozen-video profiles.
- Added invalid-mode blocking in the GUI state/handler and tests for labels/profile selection.
- Added Docker live GUI mode-selection proof: each GUI-selected mode starts and validates against its profile.
- Validation run: `python3 -m unittest discover -s tests` passed, 35 tests with 6 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 35 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 6 tests.
- Completed Milestone 8 GUI-outage-selection gate at 100% weighted coverage.
- Added GUI runtime fault controls for video, audio, captions, black video, and frozen video, applied through controlled stream restarts.
- Added restart logging, contradictory-control blocking, and safer stopped-process cleanup in GUI state.
- Added fast GUI tests for control mapping, invalid combinations, restart behavior, and start-failure recovery.
- Added Docker live GUI control test that toggles every required runtime fault off/on and validates stream state after each transition.
- Validation run: `python3 -m unittest discover -s tests` passed, 41 tests with 7 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 41 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 7 tests.
- Completed Milestone 9 runtime-fault-controls gate at 100% weighted coverage.
- Added GUI observability: last error display, intentional outage state, validation output, and downloadable diagnostics text.
- Added pipeline-exit error capture so failed child processes leave a visible error while preserving logs.
- Added GUI tests for failed feed errors, intentional outage labeling, validation output, actionable validation failures, and diagnostics export.
- Validation run: `python3 -m unittest discover -s tests` passed, 46 tests with 7 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 46 tests.
- Docker live regression run: `docker compose run --build --rm live-srt` passed, 7 tests.
- Completed Milestone 10 observability-and-troubleshooting gate at 100% weighted coverage.
- Added receiver compatibility report covering GStreamer, ffprobe, and ffplay across all required modes.
- Added FFmpeg to the Docker Linux test harness for ffprobe/ffplay compatibility checks.
- Added Docker live compatibility test for all six required profiles using ffprobe stream detection, ffplay headless smoke playback, and GStreamer receiver consumption.
- Validation run: `python3 -m unittest discover -s tests` passed, 47 tests with 8 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 47 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 8 tests.
- Completed Milestone 11 receiver-compatibility gate at 100% weighted coverage for applicable tests.
- Added `python3 -m videosim soak` timed soak harness with periodic validation, crash detection, and RSS memory sampling.
- Added short Docker live normal-feed soak proof; full 24-hour and outage soak evidence remains pending.
- Added `STABILITY_REPORT.md` with current evidence, resource notes, and pending M12 gate items.
- Validation run: `python3 -m unittest discover -s tests` passed, 52 tests with 9 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 52 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 9 tests.
- Milestone 12 remains open at 42.3% weighted coverage pending long-run soak evidence.
- Added short Docker live soak coverage for required outage profiles.
- Added Docker live GUI responsiveness proof while a feed is running.
- Updated M12 stability docs to distinguish short proofs from pending long-run gate evidence.
- Validation run: `python3 -m unittest discover -s tests` passed, 54 tests with 11 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 54 tests.
- Docker live validation run: `docker compose run --build --rm live-srt` passed, 11 tests.
- Milestone 12 remains open pending 24-hour normal/outage soak evidence and long-run GUI/resource proof.
- Added `scripts/run-m12-soak.sh` and `docker compose run --build --rm m12-soak` to collect full M12 feed soak reports for normal and outage profiles.
- Added report-output ignores for local and Docker contexts.
- Validation run: `python3 -m unittest discover -s tests` passed, 55 tests with 11 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 55 tests.
- Shortened Docker M12 soak smoke: `NORMAL_DURATION_SECONDS=8 OUTAGE_DURATION_SECONDS=8 VALIDATION_INTERVAL_SECONDS=3 docker compose run --build --rm m12-soak` passed for all six profiles.
- Full 24-hour soak evidence remains pending; the new runner is the collection path.
- Added `python3 -m videosim soak-check` to verify all six soak reports, zero crashes, validation attempts, and memory growth threshold.
- Updated `scripts/run-m12-soak.sh` to emit `summary.json` from `soak-check`.
- Validation run: `python3 -m unittest discover -s tests` passed, 58 tests with 11 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 58 tests.
- Shortened Docker M12 soak smoke with final report check passed: `NORMAL_DURATION_SECONDS=8 OUTAGE_DURATION_SECONDS=8 VALIDATION_INTERVAL_SECONDS=3 docker compose run --build --rm m12-soak`.
- Full 24-hour soak evidence remains pending; completed reports now have an automated checker.
- Added `python3 -m videosim gui-soak` and `docker compose run --build --rm m12-gui-soak` for long-run GUI responsiveness evidence collection.
- Validation run: `python3 -m unittest discover -s tests` passed, 61 tests with 11 live tests skipped locally by default.
- Docker validation run: `docker compose run --build --rm test` passed, 61 tests.
- Shortened Docker GUI soak smoke passed: `GUI_DURATION_SECONDS=8 GUI_VALIDATION_INTERVAL_SECONDS=3 GUI_POLL_INTERVAL_SECONDS=2 docker compose run --build --rm m12-gui-soak`.
- 2026-06-29: Skipped additional soak reruns per user request.
- Full 24-hour GUI responsiveness evidence remains pending; the new command is the collection path.

## 2026-06-29

- Added React/Vite frontend assets for the browser GUI with modular control, validation, and log navigation while preserving the existing form POST controls.
- Added Docker Compose `app` deployment for the GUI and SRT listener, plus a multi-stage Docker UI build.
- Added a GStreamer `clockoverlay` running clock to video-present SRT feeds and added `gstreamer1.0-x` to Debian/Docker dependency paths.
- Validation run: `python3 -m unittest tests.test_cli_video_feed tests.test_gui` passed, 26 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 62 tests with 11 live tests skipped locally by default.
- Validation run: `npm run build-ui && npm audit --omit=dev` passed with zero reported vulnerabilities.
- Docker app smoke: `docker compose up -d app`, `curl -fsS http://127.0.0.1:8080/state.json`, and `curl -fsS http://127.0.0.1:8080/static/app.js` passed.
- Docker dependency proof: installing `gstreamer1.0-x` in the running app container made `gst-inspect-1.0 clockoverlay` pass.
- Skipped check: final Docker image rebuild after adding `gstreamer1.0-x` could not complete because Docker repeatedly hung resolving base-image metadata before reaching project layers; package-level proof above verifies the added dependency.
- Added automatic React GUI preview popup for running video-capable SRT feeds using refreshed FFmpeg JPEG frames from `/preview.jpg`.
- Validation run: `python3 -m unittest tests.test_gui` passed, 19 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 66 tests with 11 live tests skipped locally by default.
- Validation run: `npm run build-ui && npm audit --omit=dev` passed with zero reported vulnerabilities.
- HTTP smoke: `python3 -m videosim gui --host 127.0.0.1 --http-port 18100 --feed-port 9912` plus `curl -fsS http://127.0.0.1:18100/preview.jpg` returned the stopped-feed preview placeholder.
- Fixed preview frame capture to use GStreamer instead of FFmpeg so local FFmpeg builds without `srt://` protocol support do not show `Protocol not found`.
- Added verbose GUI/feed logging for Docker Compose app runs, including container status, endpoint, subprocess PID, and GStreamer command output.
- Validation run: `python3 -m unittest tests.test_cli_video_feed tests.test_gui` passed, 32 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 68 tests with 11 live tests skipped locally by default.
- Changed GUI preview to render a local mode-matched frame instead of attaching a short-lived SRT receiver, because the receiver-preview path could destabilize the SRT listener.
- Validation run: `python3 -m unittest discover -s tests` passed, 69 tests with 11 live tests skipped locally by default.
- Docker runtime smoke: copied patched Python files into `videosim-app-1`, restarted the app, started normal feed, confirmed `/preview.jpg` returned BMP bytes, and `python -m videosim validate --profile profiles/srt-normal.yaml --port 9000 --json` passed while GUI state stayed running.
- Added `maxconn=10` to every generated SRT listener URI so up to 10 caller receivers can connect.
- Fixed Docker SRT stack smashing by removing the unsupported `maxconn` URI option from the GStreamer `srtsink` listener; container `gst-inspect-1.0 srtsink` exposes caller signals but no `maxconn`/max-connections property.
- Validation run: `python3 -m unittest tests.test_cli_video_feed` passed, 12 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 69 tests with 11 live tests skipped locally by default.
- Docker runtime validation: copied patched `videosim/feed.py` into `videosim-app-1`, restarted the app, started a normal feed, and `python -m videosim validate --profile profiles/srt-normal.yaml --port 9000 --json` passed.
- Docker multi-receiver smoke: two simultaneous `python -m videosim validate --profile profiles/srt-normal.yaml --port 9000 --json` clients both passed against the same SRT listener.
- Recent Docker log check: `docker compose logs --since 1m app | grep -i 'stack smashing\|Feed process exited'` returned no matches while GUI state stayed `running`.
- Skipped check: `docker compose build app` was stopped after Docker stalled resolving `node:20-slim` metadata; the running app container was patched and validated, and the source tree contains the durable fix for the next successful build.
- Added DASH as a GUI/CLI protocol option with six matching simulation profiles: normal, audio-only, video-only, no-captions, black-video, and frozen-video.
- Added DASH generation through GStreamer `dashsink`, serving `manifest.mpd`, TS segments, and WebVTT captions through the GUI HTTP server.
- Added DASH validation for MPD reachability, video/audio track presence and absence, WebVTT caption presence and absence, black-video detection, and frozen-video detection.
- Validation run: `python3 -m unittest tests.test_cli_video_feed tests.test_profiles tests.test_gui tests.test_validator` passed, 55 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 81 tests with 11 live tests skipped locally by default.
- Validation run: `npm run build-ui && npm audit --omit=dev` passed with zero reported vulnerabilities.
- Local DASH live smoke: all six DASH profiles started with `python3 -m videosim start --profile profiles/dash-*.yaml --dash-dir <tmp> --width 320 --height 180 --framerate 10` and validated with `python3 -m videosim validate --profile profiles/dash-*.yaml --dash-dir <tmp> --json`.
- Docker runtime smoke: copied patched source into `videosim-app-1`, restarted the app, started DASH normal from `POST /start protocol=dash&mode=normal`, confirmed `/dash/manifest.mpd` and `/dash/captions.vtt` served, and `python -m videosim validate --profile profiles/dash-normal.yaml --dash-dir /tmp/videosim-dash --dash-base-url http://127.0.0.1:8080/dash --json` passed.
- Skipped check: `docker compose build app` was stopped after Docker again stalled resolving `node:20-slim` metadata; runtime container was patched and validated, and committed source contains the durable build input.
- Added multi-feed GUI management with stream records for independent SRT/DASH feed instances, including create, list, read/open, update, delete, start, stop, validate, logs, endpoints, and per-stream DASH paths.
- Updated Docker Compose to publish UDP 9000-9010 for multiple GUI-managed SRT streams while DASH streams share the GUI HTTP server.
- Validation run: `python3 -m unittest tests.test_gui` passed, 29 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 85 tests with 11 live tests skipped locally by default.
- Validation run: `npm run build-ui && npm audit --omit=dev` passed with zero reported vulnerabilities.
- Docker runtime smoke: recreated `videosim-app-1` with UDP 9000-9010 published, copied patched source into the container, and started three GUI-managed streams concurrently: SRT normal on 9000, SRT video-only on 9001, and DASH black-video at `/dash/stream-3/manifest.mpd`.
- Docker validation run: `python -m videosim validate --profile profiles/srt-normal.yaml --port 9000 --json`, `python -m videosim validate --profile profiles/srt-video-only.yaml --port 9001 --json`, and `python -m videosim validate --profile profiles/dash-black-video.yaml --dash-dir /tmp/videosim-dash/stream-3 --dash-base-url http://127.0.0.1:8080/dash/stream-3 --json` all passed.
- Docker CRUDL smoke: created `stream-4`, updated it to SRT audio-only with name `Updated Temp`, confirmed it in `/state.json`, deleted it through `/streams/delete`, and confirmed it was absent while the three running streams remained listed.
- Docker multi-DASH smoke: created and started `stream-5` as DASH normal while SRT streams and DASH black-video were still running; `python -m videosim validate --profile profiles/dash-normal.yaml --dash-dir /tmp/videosim-dash/stream-5 --dash-base-url http://127.0.0.1:8080/dash/stream-5 --json` passed.
- Changed GUI startup to default to zero configured feeds and zero running feeds; users now enter through the create-feed workflow instead of an implicit primary feed.
- Validation run: `python3 -m unittest tests.test_gui` passed, 30 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 86 tests with 11 live tests skipped locally by default.
- Validation run: `npm run build-ui && npm audit --omit=dev` passed with zero reported vulnerabilities.
- Docker runtime smoke: copied patched GUI/static files into `videosim-app-1`, restarted the app, confirmed `/state.json` reported `streams: []`, created `Created SRT` via `/streams/create`, started it via `/start`, and validated `profiles/srt-normal.yaml` on port 9000 successfully.
- Added feed detail deep links at `/feeds/<stream-id>` and separated the root create/list page from selected feed detail pages so existing feed pages do not show the create-feed form.
- Validation run: `python3 -m unittest tests.test_gui` passed, 32 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 88 tests with 11 live tests skipped locally by default.
- Validation run: `npm run build-ui && npm audit --omit=dev` passed with zero reported vulnerabilities.
- Docker runtime smoke: copied patched GUI/static files into `videosim-app-1`, restarted the app, confirmed `/` exposed the create workflow with no selected feed, created `Deep SRT`, confirmed redirect to `/feeds/stream-1`, confirmed that feed detail link rendered, and confirmed the feed detail page omitted `action="/streams/create"`.
- Fixed SRT caption pipeline instability by moving the caption framerate caps before `cccombiner`, eliminating the `Missing valid caption framerate in video caps` warning that preceded Docker stack-smash exits.
- Validation run: `python3 -m unittest tests.test_cli_video_feed` passed, 17 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 88 tests with 11 live tests skipped locally by default.
- Docker runtime smoke: copied patched `videosim/feed.py` into `videosim-app-1`, restarted the app, started a normal SRT feed, validated `profiles/srt-normal.yaml` on port 9000 with video/audio/captions present, and confirmed no fresh `stack smashing`, `Feed process exited`, or missing-caption-framerate warnings.
- Added Netflix-inspired dark/light GUI theming with a persisted React theme toggle and matching no-JavaScript fallback colors.
- Validation run: `npm run build-ui` passed and rebuilt `videosim/static/app.js` and `videosim/static/app.css`.
- Validation run: `python3 -m unittest tests.test_gui` passed, 32 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 88 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- HTTP smoke: `python3 -m videosim gui --host 127.0.0.1 --http-port 18137 --feed-port 9912` served `/`, `/static/app.css`, and `/static/app.js` with dark/light theme assets.
- Skipped check: browser click verification could not run because the in-app browser runtime reported no available browser backends.
- Muted the GUI theme red accent from bright Netflix red to a darker red across React CSS, generated static CSS, and the no-JavaScript fallback.
- Validation run: `npm run build-ui` passed and rebuilt `videosim/static/app.css`.
- Validation run: `python3 -m unittest tests.test_gui` passed, 32 tests.
- HTTP smoke: restarted `python3 -m videosim gui --host 127.0.0.1 --http-port 18137 --feed-port 9912`; `/` and `/static/app.css` served the muted `#b81d24` accent with no `#e50914` references.
- Added per-feed estimated live GUI metrics: bit rate, outbound total data, uptime, and generated video frames, with React polling `/state.json` every second.
- Updated README, RUNBOOK, KNOWN_LIMITATIONS, TEST_PLAN, and ACCEPTANCE_MATRIX for GUI metrics behavior and the estimate-vs-socket-counter limitation.
- Validation run: `python3 -m unittest tests.test_gui` passed, 34 tests.
- Validation run: `npm run build-ui` passed and rebuilt `videosim/static/app.js` and `videosim/static/app.css`.
- Validation run: `python3 -m unittest discover -s tests` passed, 90 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- HTTP/live smoke: `python3 -m videosim gui --host 127.0.0.1 --http-port 18137 --feed-port 9912`, created two feeds, confirmed `/state.json` includes metrics, and started `stream-2` on UDP 9913; uptime, outbound bytes, and frame count increased across two polls while status stayed `running`.
- Fixed SRT feeds auto-stopping after a receiver disconnect by setting `srtsink wait-for-connection=false`.
- Added unit and live regression coverage for the listener staying up after a short receiver exits.
- Validation run: `python3 -m unittest tests.test_cli_video_feed` passed, 17 tests.
- Validation run: `python3 -m unittest tests.test_live_srt` passed with 11 live tests skipped locally by default.
- Live regression run: `VIDEOSIM_LIVE_SRT=1 python3 -m unittest tests.test_live_srt.LiveSrtTest.test_receiver_detects_audio_video_captions_and_feed_restarts` passed.
- Validation run: `python3 -m unittest discover -s tests` passed, 90 tests with 11 live tests skipped locally by default.
- Changed the React GUI primary view to an active-feed table with endpoint, status, metrics, actions, and per-row preview thumbnails.
- Moved feed creation into a modal and added a full preview dialog opened from row thumbnails.
- Added per-stream preview URLs at `/feeds/<stream-id>/preview.jpg` without changing selected GUI state.
- Validation run: `npm run build-ui` passed and rebuilt `videosim/static/app.js` and `videosim/static/app.css`.
- Validation run: `python3 -m unittest tests.test_gui` passed, 35 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 91 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- HTTP smoke: `python3 -m videosim gui --host 127.0.0.1 --http-port 18138 --feed-port 9912`, created feeds, confirmed root table HTML and `/state.json` preview URLs, and started `stream-2` on UDP 9913; `/feeds/stream-2/preview.jpg` returned a 640x360 BMP while status was `running`.
- Skipped check: browser click verification could not run because the browser runtime reported no available browser backends.

## 2026-07-07

- Wired the React GUI to the `design_docs` model: top bar wordmark, active-feed table/detail layout, IBM Plex tokens, warm dark/light surfaces, amber actions, mono endpoint/log/metric wells, and dot-plus-label status badges.
- Updated the no-JavaScript fallback GUI styling, README, RUNBOOK, and ACCEPTANCE_MATRIX to match the active-feed table and design-system choices.
- Added GUI regression coverage for the production design tokens and root light-theme hook.
- Validation run: `npm run build-ui` passed and rebuilt `videosim/static/app.js` and `videosim/static/app.css`.
- Validation run: `python3 -m unittest tests.test_gui` passed, 36 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 92 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- HTTP smoke: `python3 -m videosim gui --host 127.0.0.1 --http-port 18149 --feed-port 9912` served `/`, `/static/app.css`, `/static/app.js`, and `/state.json`; saved responses exposed the design-token hooks and initial state.
- Skipped check: visual browser screenshot verification could not run because the in-app browser runtime listed no available browser backends.
- Added stream detail traffic graphs for bit rate and outbound data, backed by the React `/state.json` polling loop and a rolling five-minute client-side metrics window.
- Validation run: `python3 -m unittest tests.test_gui` passed, 37 tests.
- Validation run: `npm run build-ui` passed and rebuilt `videosim/static/app.js` and `videosim/static/app.css`.
- Validation run: `python3 -m unittest discover -s tests` passed, 93 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- HTTP/live metrics smoke: `python3 -m videosim gui --host 127.0.0.1 --http-port 18151 --feed-port 19012`, created `GraphSmoke`, confirmed served static assets include traffic graph hooks, started `stream-1`, and two `/state.json` polls showed `status=running`, bit rate `4342800`, uptime `7.6 -> 15.0`, and outbound bytes `4126828 -> 8135459`.
- Skipped check: browser screenshot verification was not run; HTTP/static asset and live state smoke covered the graph path in this headless run.
- Added a separate `videosim monitor` app and Docker Compose `monitor` service that polls GUI state, validates running feeds, writes active/steady alarms plus event audit history, and repeats active alarm events every 5 seconds.
- Added GUI alarm review panels, monitor state exposure in `/state.json`, TR 101 290 monitor catalogue documentation, and tests for alarm cadence, missing video/audio detection, and GUI alarm payload rendering.
- Validation run: `python3 -m unittest tests.test_monitor tests.test_gui tests.test_cli_video_feed tests.test_validator tests.test_docs_contract` passed, 65 tests.
- Validation run: `npm run build-ui` passed and rebuilt `videosim/static/app.js` and `videosim/static/app.css`.
- Validation run: `python3 -m unittest discover -s tests` passed, 96 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- HTTP/monitor smoke: `VIDEOSIM_MONITOR_STATE=/private/tmp/videosim-monitor-smoke.json python3 -m videosim gui --host 127.0.0.1 --http-port 18152 --feed-port 19022`, `python3 -m videosim monitor --gui-state-url http://127.0.0.1:18152/state.json --state-path /private/tmp/videosim-monitor-smoke.json --once`, and `/state.json` confirmed 22 monitor definitions, zero active alarms after recovery, and alarm raise/clear audit events from the sandbox-blocked first monitor poll.
- Skipped check: full live monitor validation against broken media streams is pending the parser-backed TR 101 290 implementation; this slice validates monitor state, cadence, UI exposure, and local process/container wiring.
- Added parser-backed TR 101 290 priority 1/2 MPEG-2 TS checks for sync, sync byte, PAT, continuity count, PMT, PID, transport error, CRC, PCR, PTS, and CAT indicators, and wired those indicators into monitor alarms.
- Validation run: `python3 -m unittest tests.test_tr101 tests.test_monitor` passed, 6 tests.
- Validation run: `python3 -m unittest tests.test_gui tests.test_cli_video_feed tests.test_validator` passed, 59 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 100 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Skipped check: no generated malformed live MPEG-TS fixtures exist yet for Docker end-to-end proof of each TR 101 290 indicator; parser behavior is covered by synthetic TS unit tests.
- Added monitor regression coverage proving DASH `.ts` segment sampling flows through the TR 101 290 analyzer into monitor issues.
- Validation run: `python3 -m unittest tests.test_monitor tests.test_tr101 tests.test_live_srt` passed, 18 tests with 11 live tests skipped locally by default.
- Validation run: `python3 -m unittest discover -s tests` passed, 101 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Skipped check: attempted local `VIDEOSIM_LIVE_SRT=1` monitor proof could not be used because the local SRT receiver pipeline timed out even for a normal feed; Docker malformed-stream fixture proof remains the tracked gap.
- Added parser-backed TR 101 290 priority 3 PSI/SI checks for NIT, SI repetition, unreferenced PID, SDT, EIT, EIT P/F pairing, RST, and TDT indicators, plus monitor catalogue entries for the remaining T-STD buffer/data-delay indicators.
- Updated monitoring docs, test plan, gaps, limitations, runbook, README, and acceptance matrix for the priority 3 parser-backed scope and T-STD pending scope.
- Validation run: `python3 -m unittest tests.test_tr101 tests.test_monitor` passed, 11 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 105 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Skipped check: malformed live SRT/DASH fixture streams for end-to-end proof of every TR 101 290 indicator remain pending; parser behavior is covered by synthetic TS unit tests.
- Added CAT CA descriptor parsing so TR 101 290 unreferenced-PID checks exempt CAT-referenced CA PIDs instead of false-positive alarming on valid conditional-access metadata.
- Validation run: `python3 -m unittest tests.test_tr101` passed, 7 tests.
- Validation run: `python3 -m unittest tests.test_tr101 tests.test_monitor` passed, 12 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 106 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Added monitor-level regression coverage proving `run_monitor_once` samples malformed DASH `.ts` segments and writes TR 101 290 alarms/events through the normal monitor state path.
- Updated `TEST_GAPS.md` to distinguish covered DASH segment monitor sampling from the remaining malformed live SRT/DASH Docker fixture gap.
- Validation run: `python3 -m unittest tests.test_monitor` passed, 6 tests.
- Validation run: `python3 -m unittest tests.test_monitor tests.test_tr101` passed, 13 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 107 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Added parser-backed TR 101 290 priority 3 T-STD timing checks for `Buffer_error`, `Empty_buffer_error`, and `Data_delay_error` using sample byte-rate arrival time and PES PTS.
- Updated monitoring docs, test plan, gaps, limitations, runbook, README, and acceptance matrix for the T-STD timing approximation.
- Validation run: `python3 -m unittest tests.test_tr101 tests.test_monitor` passed, 15 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 109 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Expanded DASH TR 101 290 sampling to the latest 15 `.ts` segments so monitor analysis can cover 10s and 30s SI timing windows.
- Added malformed DASH fixture coverage proving every implemented TR 101 290 indicator can surface through `tr101_issues_for_stream` as a monitor issue.
- Updated monitoring docs, test plan, and test gaps for all-indicator DASH fixture monitor proof.
- Validation run: `python3 -m unittest tests.test_monitor` passed, 7 tests.
- Validation run: `python3 -m unittest tests.test_monitor tests.test_tr101` passed, 16 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 110 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Added a `monitor-fixtures` Docker Compose service that runs the malformed DASH/TR 101 290 monitor fixture suite inside the Linux container image.
- Updated README, RUNBOOK, TEST_PLAN, and TEST_GAPS with the new Docker monitor fixture gate and the narrower remaining live malformed-stream proof gap.
- Validation run: `python3 -m unittest tests.test_monitor tests.test_tr101` passed, 16 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 110 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Skipped check: `docker compose run --build --rm monitor-fixtures` was attempted but Docker stalled for roughly two minutes while loading image metadata/build layers; the command was interrupted before reaching project tests.
- Containerized monitor fixture proof: `docker compose run --rm --pull never -v /Users/sagarmistry/Projects/videosim:/app test python -m unittest tests.test_monitor tests.test_tr101` passed, 16 tests, using the existing Linux test image with current source bind-mounted.

## 2026-07-07

- Verified existing stream detail traffic graphs for bit rate and outbound data, backed by the React `/state.json` polling loop and a rolling five-minute client-side metrics window.
- Validation run: `python3 -m unittest tests.test_gui` passed, 38 tests.
- Validation run: `npm run build-ui` passed with no source or generated asset diff.
- Completed monitor objective audit: separate monitor app/container wiring, feed validation alarms, TR 101 290 monitor catalogue, 5-second alarm repeat behavior, clear-to-steady state, GUI alarm review, event audit history, and extensibility docs are implemented and covered by current tests/docs.
- Validation run: `python3 -m unittest tests.test_monitor tests.test_tr101 tests.test_gui tests.test_docs_contract` passed, 58 tests.
- Validation run: `npm run build-ui` passed.
- Validation run: `docker compose config --quiet` passed.
- Containerized monitor fixture proof: `docker compose run --rm --pull never -v /Users/sagarmistry/Projects/videosim:/app test python -m unittest tests.test_monitor tests.test_tr101` passed, 16 tests, using the existing Linux test image with current source bind-mounted.
- Validation run: `python3 -m unittest discover -s tests` passed, 110 tests with 11 live tests skipped locally by default.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `git diff --check` passed.
- Skipped check: `docker compose run --rm --pull never monitor-fixtures` still attempted a service image build and stalled for roughly one minute while loading Docker base-image metadata; the command was interrupted before project tests. The bind-mounted existing Linux test image proof above covered the same monitor fixture suite.
- Added audio loudness monitor alarms using FFmpeg `ebur128` measurements for ITU-R BS.1770 measurement availability, EBU R 128 integrated loudness/true peak, and ATSC A/85 integrated loudness.
- Updated monitoring docs, runbook, test plan, gaps, limitations, README, and acceptance matrix for loudness alarms and the short-sample limitation.
- Validation run: `python3 -m unittest tests.test_loudness tests.test_monitor` passed, 14 tests.
- Validation run: `python3 -m unittest tests.test_gui tests.test_docs_contract` passed, 42 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 117 tests with 11 live tests skipped locally by default.
- Validation run: `npm run build-ui` passed with no generated asset diff.
- Validation run: `docker compose config --quiet` passed.
- Containerized monitor/loudness fixture proof: `docker compose run --rm --pull never -v /Users/sagarmistry/Projects/videosim:/app test python -m unittest tests.test_loudness tests.test_monitor tests.test_tr101` passed, 23 tests, using the existing Linux test image with current source bind-mounted.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `git diff --check` passed.
- Added GUI feed frame-rate selectors for 23.97, 24, 25, 50, 59.94, and 60 fps, with 59.94 fps as the GUI default and legacy 30 fps profile/config support preserved.
- Added shared frame-rate normalization/caps helpers so fractional rates use rational GStreamer caps and matching caption caps/keyframe intervals.
- Added the `Video frame rate match` monitor alarm using FFprobe-measured video rates against the configured stream rate, including DASH segment probing and audio-only skip behavior.
- Updated README, runbook, monitoring docs, acceptance matrix, test plan, gaps, and known limitations for selected frame-rate validation and alert behavior.
- Validation run: `python3 -m unittest tests.test_framerate tests.test_cli_video_feed tests.test_profiles tests.test_gui tests.test_monitor` passed, 86 tests.
- Validation run: `npm run build-ui` passed.
- Validation run: `python3 -m unittest discover -s tests` passed, 128 tests with 11 live tests skipped locally by default.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Containerized frame-rate monitor/GUI proof: `docker compose run --rm --pull never -v /Users/sagarmistry/Projects/videosim:/app test python -m unittest tests.test_framerate tests.test_monitor tests.test_gui` passed, 58 tests, using the existing Linux test image with current source bind-mounted.
- Validation run: `git diff --check` passed.
- Added per-stream alert profiles with selected monitor IDs and alarm delay seconds, exposed on stream detail pages and in `/state.json`.
- Added monitor-side alert filtering and pending-delay handling so disabled alerts clear and enabled issues must persist before raising.
- Updated README, runbook, monitoring docs, goal plan, acceptance matrix, test plan, gaps, and limitations for alarming startup and process-local alert profiles.
- Validation run: `python3 -m unittest tests.test_gui tests.test_monitor` passed, 56 tests.
- Validation run: `npm run build-ui` passed.
- Validation run: `python3 -m unittest discover -s tests` passed, 131 tests with 11 live tests skipped locally by default.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Containerized alert-profile monitor/GUI proof: `docker compose run --rm --pull never -v /Users/sagarmistry/Projects/videosim:/app test python -m unittest tests.test_gui tests.test_monitor` passed, 56 tests, using the existing Linux test image with current source bind-mounted.
- Validation run: `git diff --check` passed.
- Fixed the alert-profile lifecycle UI so stream detail pages always show the built-in monitor catalogue before the monitor service has written state.
- Added alert-profile lifecycle controls for save selected, enable all, disable all, plus per-alert Off/On/Pending/Active status and monitor connectivity state.
- Moved monitor catalogue definitions into a shared module used by both the GUI and monitor, and exposed pending alert state in `/state.json`.
- Updated README, runbook, monitoring docs, acceptance matrix, test plan, and test gaps for the always-available alert lifecycle UI.
- Validation run: `python3 -m unittest tests.test_gui tests.test_monitor` passed, 58 tests.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Validation run: `npm run build-ui` passed.
- Validation run: `python3 -m unittest discover -s tests` passed, 133 tests with 11 live tests skipped locally by default.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Containerized alert lifecycle UI proof: `docker compose run --rm --pull never -v /Users/sagarmistry/Projects/videosim:/app test python -m unittest tests.test_gui tests.test_monitor` passed, 58 tests, using the existing Linux test image with current source bind-mounted.
- Validation run: `git diff --check` passed.
- Fixed feed shutdown after `KeyboardInterrupt` so a closed caption stdin pipe cannot surface as `BrokenPipeError` and make GUI stop/restart flows report a feed process failure.
- Validation run: `python3 -m unittest tests.test_cli_video_feed` passed, 19 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 134 tests with 11 live tests skipped locally by default.
- Containerized shutdown regression proof: `docker compose run --rm --pull never -v /Users/sagarmistry/Projects/videosim:/app test python -m unittest tests.test_cli_video_feed` passed, 19 tests, using the existing Linux test image with current source bind-mounted.
- Validation run: `git diff --check` passed.
- Docker alert simulation: inspected running `app` and `monitor` containers, patched the running app container with current source/profile files, created `AlertSmoke`, enabled all alerts with zero delay, and toggled SRT modes `normal`, `audio_only`, `video_only`, `no_captions`, `black_video`, and `frozen_video`.
- Fixed stale GUI error state where a previous feed failure could be copied back into a successfully restarted stream; final Docker state reported `frozen_video`, `running`, and `lastError=none`.
- Fixed live frozen-video generation by replacing the unreliable `imagefreeze` branch with a static red live source and lossless x264 settings for frozen profiles; container validation passed for `profiles/srt-frozen-video.yaml`.
- Verified alert UI presence: `/feeds/stream-1` contained `Monitor alarms`, active alarm names, and alert profile lifecycle controls while `/state.json` reported monitor connected and active alarms for `AlertSmoke`.
- Validation run: `python3 -m unittest tests.test_profiles tests.test_cli_video_feed tests.test_gui` passed, 72 tests.
- Validation run: `docker compose exec app python -m videosim validate --profile profiles/srt-frozen-video.yaml --port 9000 --framerate 59.94 --json` passed with `frozen_video=true`.
- Validation run: `python3 -m unittest discover -s tests` passed, 136 tests with 11 live tests skipped locally by default.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Skipped checks: full Docker image rebuild and browser screenshot automation were not run; the running containers were updated with source/profile files and verified through live Docker HTTP, logs, monitor state, and validator checks.
- Fixed explicit alert-profile essence checks so selecting `Video present` expects video even when the stream mode is an intentional audio-only profile; default/all-enabled audio-only behavior remains mode-aware.
- Live Docker verification: patched and restarted the running monitor container, confirmed Feed 2 (`audio_only`) with only `essence_video_present` enabled raised an active `Video present` alarm and `/feeds/stream-2` displayed `Expected video is absent`.
- Validation run: `python3 -m unittest tests.test_monitor` passed, 19 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 138 tests with 11 live tests skipped locally by default.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Added a SQLite-backed feed registration store behind a `FeedRegistrationStore` boundary, with CLI GUI startup using `VIDEOSIM_DB_PATH` or the default user data path for persisted feed definitions and alert profiles.
- Added external SRT and external DASH feed registration in the GUI, including URL validation, persisted source/URL fields, monitorable external status, validation through the registered endpoint, and UI behavior that hides generated-feed start/stop/fault controls for external feeds.
- Added external DASH URL validation support for reachable MPDs with common `SegmentURL` or `SegmentTemplate` media references, plus external DASH frame-rate/loudness probe input support.
- Updated README, runbook, known limitations, and test plan for SQLite registration and bring-your-own SRT/DASH feed alerting.
- Validation run: `python3 -m unittest tests.test_feed_store tests.test_gui tests.test_monitor tests.test_validator tests.test_cli_video_feed tests.test_framerate tests.test_loudness` passed, 101 tests.
- Validation run: `npm run build-ui` passed.
- Validation run: `python3 -m unittest discover -s tests` passed, 146 tests with 11 live tests skipped locally by default.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Made generated/external feed forms source-aware: generated forms hide external URL, external forms hide mode and frame-rate fields, and fallback HTML uses separate generated/external create forms.
- Changed external feed validation and monitor config to passive ingest mode so external feeds report whatever tracks arrive and only raise absence alarms for alert checks explicitly enabled on that feed.
- Validation run: `python3 -m unittest tests.test_gui tests.test_monitor tests.test_validator tests.test_cli_video_feed tests.test_feed_store` passed, 94 tests.
- Validation run: `npm run build-ui` passed.
- Validation run: `python3 -m unittest discover -s tests` passed, 147 tests with 11 live tests skipped locally by default.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Fixed SQLite feed registration use from GUI request threads by allowing cross-thread SQLite access inside the store boundary and serializing store operations with a lock.
- Added a feed-store regression proving a store created on one thread accepts a feed upsert from another request-like thread.
- Validation run: `python3 -m unittest tests.test_feed_store tests.test_gui` passed, 47 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 148 tests with 11 live tests skipped locally by default.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Made the stream-page event audit collapsible and added a clear action that removes only the selected stream's monitor events from the shared monitor state file.
- Added GUI regression coverage for the clear action and React event-audit controls.
- Validation run: `python3 -m unittest tests.test_gui` passed, 46 tests.
- Validation run: `npm run build-ui` passed.
- Validation run: `python3 -m unittest discover -s tests` passed, 149 tests with 11 live tests skipped locally by default.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Added the first master/worker control-plane slice: worker assignment/report APIs in the GUI master, a `python -m videosim worker` polling loop that reuses monitor checks, and a sample Compose `worker` service.
- Added `docs/distributed-architecture.md` and updated README, runbook, test plan, and known limitations for distributed monitoring, current round-robin assignment, in-memory worker registry, and deferred HA/auth/storage work.
- Added regression coverage for worker assignment splitting, generated DASH master monitor endpoints, worker report merge isolation, worker CLI dispatch, and worker loop polling/reporting.
- Validation run: `python3 -m unittest tests.test_gui tests.test_monitor tests.test_worker tests.test_cli_video_feed` passed, 94 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 155 tests with 11 live tests skipped locally by default.
- Validation run: `docker compose config --quiet` passed.
- Validation run: `git diff --check` passed.
- Validation run: `npm run build-ui` passed.
- Skipped checks: live multi-container worker smoke was not run; worker behavior was covered by unit tests and Compose config validation in this slice.
- 2026-07-08: Reworked the top-level README into a brief public-facing project overview with quick start, supported modes, verification, and links to deeper docs.
- 2026-07-08: Moved implementation-heavy README content into `docs/implementation.md` and moved optional root reports to `docs/monitoring.md`, `docs/compatibility-report.md`, and `docs/stability-report.md`.
- 2026-07-08: Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- 2026-07-08: Validation run: repository-shape checks confirmed optional report docs live under `docs/` and stale root report links are gone.
- 2026-07-08: Skipped checks: full unit suite and live Docker gates were not run; this was documentation-only and covered by the docs contract plus link/reference checks.

## 2026-07-11

- Added `docs/production-readiness-audit.md`, an evidence-backed static audit of the current control-plane/worker slice and a production target architecture, explicit 1,000/5,000/10,000-stream demand models, security/HA/operations requirements, protocol-specific guidance, FMEA, phased migration/rollback gates, and measurable verification plan.
- Traced feed persistence, local runtime orchestration, worker registration/assignment, serial monitor execution, report ingestion, alarm/event state, DASH serving, and browser polling to repository code; documented a high-severity retained/out-of-scope worker-state merge defect without changing application behavior.
- Added audit discoverability and scale-claim disclaimers to `README.md`, `docs/distributed-architecture.md`, and `KNOWN_LIMITATIONS.md`.
- Independent fresh-context reviews checked current distributed behavior and the audit against all 11 requested requirements; follow-up edits added report-scope integrity analysis, retry/API contracts, complete capacity formulas/storage dimensions, additional failure modes, measurable rollback controls, and a reproducible evidence manifest.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 155 tests with 11 live SRT tests skipped by their default environment gate.
- Validation run: `npm run build-ui` passed; generated assets had no tracked diff.
- Validation run: `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: `docker compose config --quiet` passed.
- Validation run: audit path/link verification checked 16 referenced repository paths with no missing paths; `git diff --check` passed.
- Skipped checks: live SRT/Docker media gates were not rerun because this task changes documentation only and the full non-live regression suite passed. Production-scale load, security, failover, chaos, and DR tests could not run because the target architecture, representative workload, credentials, and production-like 1,000/5,000/10,000-stream environment do not yet exist; the audit labels those claims unverified and defines the evidence needed.
- Began the full distributed-architecture implementation roadmap with the reversible F0 foundation tracked in `docs/distributed-implementation-progress.md`.
- Added strict-by-default `videosim.worker/v1` contracts with a control-plane process instance, monotonic assignment generation, opaque per-worker assignment token, process-restart/ABA/config-change fencing, and explicit temporary legacy compatibility mode.
- Fixed retained/out-of-scope worker report contamination: the server now derives current ownership, validates every alarm/event/pending/probe-metric item, recomputes scoped metric aggregates, reports rejected/dropped IDs, and never acknowledges monitor-state persistence failure.
- Added worker assignment-state pruning, bounded HTTP 409 refetch, and an independent heartbeat that keeps workers registered while serial probe batches exceed the current worker TTL.
- Added monotonic latest-batch probe instrumentation for success, issue, error, timeout, and skipped outcomes; worker metric state is bounded, scoped, and retired for inactive workers on the next report.
- Serialized stream allocation and assignment snapshots, and closed the concurrent start/delete race so a deleted feed cannot leave an unmanaged media subprocess.
- Added real HTTP worker API tests for 200/409/422/503 behavior, stale/restart/ABA/token/type fencing tests, heartbeat-over-TTL coverage, empty-assignment pruning, metric scoping, persistence failure, and deterministic lifecycle/allocation/assignment concurrency tests.
- Added `python3 -m videosim control-plane-benchmark`, which validates unique assignment coverage and report acceptance in-process while explicitly reporting `mediaProbesExecuted=false` and `capacityCertified=false`.
- Independent review found and drove fixes for long-batch TTL expiry, metric scope, unsafe default legacy mode, persistence acknowledgements, stream mutation races, and start/delete process leakage; final foundation review reported no blocker/high findings.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 189 tests with 11 live SRT tests skipped by their default environment gate.
- Validation run: `npm run build-ui` passed with no intended frontend source change; `npm audit --omit=dev` reported zero vulnerabilities.
- Validation run: `docker compose config --quiet` and `git diff --check` passed.
- Foundation benchmark: `python3 -m videosim control-plane-benchmark --streams 1000 --workers 10 --iterations 3 --warmup-iterations 1 --seed 17 --json` passed 30 report acceptances with p50/p95/p99 cycle durations 38.163/38.870/38.933 ms on this development machine. This is control-plane-only proxy evidence, not media capacity certification.
- Skipped checks: live multi-container SRT/DASH worker probing and Docker image rebuild were not run in this foundation slice. Durable authenticated leases, database/broker ingestion, backpressure, HA/failover, security, restore, and production-like 1,000/5,000/10,000 media admission remain active F1-F7 gates and are not claimed complete.
- Selected Docker Compose/VM as the production deployment target and mTLS workers plus OIDC operators as its identity model.
- Added `docker-compose.production.yml`, Nginx TLS/mTLS proxy configuration, oauth2-proxy OIDC configuration, internal-only app networking, separate operator/worker rate limits, Secure SameSite cookies, per-request login CSRF cookies, and exact same-origin validation for admin mutations.
- Added trusted-proxy application enforcement: a 32+ character proxy secret, certificate-derived worker ID matching, OIDC viewer/admin authorization, public health/readiness endpoints, request-local viewer feed selection, structured authorization audit events, and strict versioned worker reports.
- Added explicit HTTPS worker assignment origin support so generated DASH URLs do not infer/downgrade from Host headers, plus worker CA/client certificate CLI options.
- Added 1 MiB proxy/app body limits, bounded report collections/identifiers, controlled malformed worker JSON handling, private-destination/suffix policy, permanent denial of loopback/link-local/multicast/reserved/unspecified destinations, and inline-credential rejection.
- Added bounded exponential jitter retries for transient worker assignment/report failures while preserving distinct HTTP 409 assignment-refetch semantics.
- Added production security docs, environment template, ignored certificate directory, and a local-only CA/server/worker certificate generator with strict CA key-usage extensions.
- Added security/control-plane tests for proxy-secret spoofing, mTLS identity mismatch, OIDC viewer/admin roles, CSRF, non-mutating viewer GETs, HTTPS DASH assignment URLs, destination policy, request/report limits, malformed IDs/UTF-8, TLS context, retry policy, and production handler responses.
- Independent security review found and drove fixes for missing CSRF protection, HTTP downgrade on generated DASH assignments, viewer GET mutation, malformed worker IDs, overly broad private-address opt-in, and the concurrent proxy/header boundary; final review reported no blocker/high findings.
- Validation run: `python3 -m unittest tests.test_docs_contract` passed, 4 tests.
- Validation run: `python3 -m unittest discover -s tests` passed, 211 tests with 11 live SRT tests skipped by their default environment gate.
- Validation run: `npm run build-ui` and `npm audit --omit=dev` passed with zero reported vulnerabilities.
- Validation run: default and production `docker compose ... config --quiet` passed with required test environment values.
- Validation run: `bash -n scripts/generate-dev-mtls-certs.sh`, OpenSSL server/worker chain verification, and Nginx container `nginx -t` passed.
- The first Python mTLS worker smoke exposed that the development CA lacked a critical certificate-signing key-usage extension even though curl/OpenSSL accepted it. The generator was fixed, certificates regenerated, and a final real `python3 -m videosim worker --once` call through Nginx mTLS passed; the same worker certificate requesting `worker-2` was rejected with HTTP 403.
- Skipped checks: a real organization OIDC login/group-claim flow, certificate rotation/revocation, VM firewall deployment, redirect/DNS-rebinding tests, durable identity/audit storage, and multi-tenant grants remain explicit F1/F2/F4 gates. No production security certification is claimed from local proxy tests.
- Began F2 with the selected PostgreSQL-authority/NATS-JetStream architecture. Added a checksum/name-verified advisory-locked migration runner; bounded-pool PostgreSQL feed, worker, offered/acknowledged lease, fenced report/result, current-state/alarm, audit, transactional-outbox, and consumer-inbox repositories; exact JetStream configuration validation; an at-least-once publisher; dead-event requeue; and production Compose storage/init/publisher services pinned to inspected image digests.
- Added immediate config/incarnation lease revocation, database-time worker heartbeat/lease expiry, explicit lease acknowledgement, authoritative feed-version checks, bounded/reordered observation-time rejection, result/report/outbox payload hashes, typed duplicate dispositions, and the rule that only explicit unhealthy/healthy evidence can raise/clear while unknown/stale/skipped/error/timeout preserves alarm state.
- Added idempotent SQLite feed cutover, destructive-restore source/target/live-identity interlocks, explicit empty-versus-retained broker recovery modes, restored-lease expiry, custom-format backup validation, and a repeatable-read streaming semantic comparator. Reconciled the runbook to require quiesce → storage/migration → import/verify → app/proxy start.
- Independent F2 code and architecture reviews found and drove fixes for Docker build-context secret inclusion, pre-reconcile incarnation/config fencing windows, result-ID conflict handling, false alarm clears on inconclusive evidence, missing heartbeat/acknowledgement and observation-time contracts, outbox ID drift, migration ordering/status drift, destructive restore targeting, broker recovery semantics, cutover ordering, snapshot consistency, and unsupported concurrency/evidence claims. At that repository-foundation checkpoint, HTTP worker-v2/read/audit/consumer/HA/scale gates remained open; the later v2 cutover entry below supersedes only the worker-v2 portion.
- Integration validation: disposable `postgres:17-alpine@sha256:c7526c0f6c3f30260a563d7bcf8ad778effac59a44f8ffa86678c35418338609` (PostgreSQL 17.9) and `nats:2.11-alpine@sha256:e4bf19f15fd3218814a4e3c9e0064e1334bd8aa20d5984b9f1a0afd084f8cc00` (NATS 2.11.17) ran `VIDEOSIM_TEST_POSTGRES_URL=... VIDEOSIM_TEST_NATS_URL=... /tmp/videosim-durable-venv/bin/python -m unittest tests.test_postgres_store tests.test_nats_publisher -v`; all 28 tests passed. Coverage included migration up/down/checksum/unknown-version behavior, immediate config/incarnation fences, offered/acknowledged leases, per-epoch sequence restart after config/incarnation/expiry/restore transitions, stale heartbeats, observation clocks, inconclusive alarm state, immutable-ID drift, register/reconcile and feed-update/ack/ingest races, simultaneous report serialization, concurrent disjoint outbox claims, retry/owner/dead recovery, exact JetStream policy, publish acknowledgement, and immediate broker dedup replay.
- Restore validation: `scripts/postgres-backup.sh`, `scripts/postgres-restore.sh`, and `scripts/verify-postgres-restore.py` passed against fresh target databases in both `empty` and `retained` broker modes. The recorded empty-broker run completed in 1 second with a 117,045-byte custom-format backup, expired 69 leases, requeued 261 non-dead outbox events, advanced informational generation 1→2, and matched semantic SHA-256 `5642c4f929a6edc4447273661c4c7c9b3a6890eeeecc0ccc724b82c07560b6e2`; the verifier reported `passed=true` across migrations, 81 feeds, 69 workers, 69 leases, 167 reports, 99 results, current state/alarms/events/audit, 261 outbox events, and the consumer inbox. Publishing that empty-broker restore into a separate fresh JetStream produced exactly 261 messages; a separate retained-broker restore also passed semantic comparison.
- Regression validation: `python3 -m unittest discover -s tests` passed 249 tests with 37 environment-gated skips (11 existing live-media tests and 26 PostgreSQL/NATS integration tests when service URLs are absent). `python3 -m unittest tests.test_docs_contract`, Python compilation, shell syntax, `git diff --check`, production Compose rendering, `npm run build-ui`, `npm audit --omit=dev`, and `pip-audit -r requirements.txt` passed; npm and Python dependency audits reported no known vulnerabilities.
- Functional smoke: the CLI migration-status and JetStream initializer succeeded, SQLite import reported one changed feed then zero on retry, and a PostgreSQL-backed GUI served `/readyz` and `/state.json` locally.
- Skipped/blocked checks: two Docker image builds were attempted but timed out while Docker Hub metadata for `node:20-slim` was unavailable; local venv, Compose, PostgreSQL/NATS, UI, and dependency checks covered changed components but do not replace a successful image rebuild. Live SRT/DASH media gates were not rerun because the media path did not change. Broker ack ambiguity beyond the two-minute dedup window, atomic consumer inbox/projection replay, DB/broker outage matrices under load, schema upgrade under load, production PITR/RPO/RTO, worker HTTP-v2 cutover, durable operator reads/security-audit wiring, HA, and 1,000/5,000/10,000 admission remained open at that foundation checkpoint; later worker-v2 work is recorded below.
- Added F2 PostgreSQL worker API v2 cutover with migration `002_worker_projection_fence.sql`. PostgreSQL mode now requires a per-process worker incarnation, rejects v1 downgrade, returns durable offered leases, requires exact offer acknowledgement, renews only matching unexpired active leases, and fences `probe.*` result ingestion by tenant, identity, incarnation, epoch, config, per-lease sequence, expiry, and observation time. SQLite retains v1 only as a trusted-lab compatibility path.
- Added per-stream durable JSON-shadow projection fences keyed by report UUID, payload hash, epoch/config/sequence, worker incarnation, and current live lease. DB result/outbox plus pending fence commit before JSON; a same-report retry can apply a still-current pending shadow, while newer reports, reassignment, expiry/reoffer, config revocation, stale heartbeat, or incarnation replacement make an old retry a no-op/superseded. This is a no-regression shadow, not the deferred durable operator alarm/read projection.
- Added worker client v2 negotiation, offer acknowledgement, stable report retry IDs, per-lease sequence counters, bounded 409 refetch, and v1 regression compatibility. Added PostgreSQL HTTP tests for real `run_worker --once`, fresh-incarnation takeover protection, non-resurrecting expiry, JSON-write retry, delayed newer/reassigned/expired/config-revoked shadow retries, concurrent assignment polls, two-worker handoff/stale-owner rejection/survivor pickup, and migration-002 reversal.
- Independent worker-v2 reviews found and drove fixes for expired active lease resurrection, global instead of per-lease sequence handling, stale duplicate JSON projection after newer/reassigned authority, pending-shadow retry after reassignment/config/expiry, lock-order inversion risk, and stale status/evidence wording. Final review found no remaining blocker/high code finding; actual alarm/read projection, consumers, security-audit wiring, HA, DR certification, and scale admission remain explicitly open.
- Integration validation: fresh disposable PostgreSQL database `videosim_v2final` applied migrations 001 and 002 and, with NATS 2.11.17, ran `VIDEOSIM_TEST_POSTGRES_URL=... VIDEOSIM_TEST_NATS_URL=... /tmp/videosim-durable-venv/bin/python -m unittest tests.test_postgres_store tests.test_nats_publisher tests.test_worker_v2 -v`; all 43 tests passed. This includes v2 real HTTP/worker, proxy-verified worker identity enforcement, DB-fenced projection/reassignment, and existing transactional-outbox cases.
- Restore validation after migration 002: custom backup/empty-broker restore passed in 1 second with a 55,156-byte dump, migration identities 001/002, informational generation 1→2, 11 expired leases, 46 requeued non-dead outbox events, and semantic SHA-256 `b8515317d79fadeeadd84058dc73dfedb0dd8c405de5821edbaeb307ad305fa8`. Semantic comparison passed across 17 feeds, 15 workers/leases, 33 reports, 19 results, current state/alarm/audit rows, 46 outbox events, the empty consumer inbox, and `worker_projection_state`; publishing the empty-broker restore into a fresh JetStream produced exactly 46 messages.
- Regression validation: `python3 -m unittest discover -s tests` passed 268 tests with 52 environment-gated skips (live media and PostgreSQL/NATS suites when URLs are absent). `python3 -m compileall -q videosim tests scripts/verify-postgres-restore.py`, shell syntax, `git diff --check`, focused worker/GUI/API tests, and migration status for versions 001/002 passed. Live Docker image rebuild and media gates remain skipped for the previously recorded Docker Hub/network reason; no scale or HA claim follows.
- Added F2 direct PostgreSQL monitor-alarm/operator-read cutover selected for the next vertical slice. Migration `003_direct_monitor_projection.sql` adds pending alert state, `current_alarms.last_event_at`, and read/retention indexes. Worker v2 now sends bounded catalog monitor observations alongside `probe.*` metrics; PostgreSQL validates catalog/scope/status/uniqueness, deterministically persists observation results, and atomically projects pending/current alarms, immutable raised/active/cleared/suppressed edges, and outbox records. `probe.*` remains operational evidence and is never exposed as a UI alarm.
- The PostgreSQL projector owns alert delay/repeat timing from database time, suppresses alarms explicitly made inapplicable by profile or feed-mode configuration, preserves active/pending state on omitted/unknown/stale/skipped/error/timeout evidence, and bounds per-stream event history to 1,000 rows and seven days by default without pruning immutable outbox records. Production Compose adds `monitor-history-pruner` for bounded global age sweeps so inactive streams also expire; its CLI one-shot/batch behavior is tested. Durable `/state.json` reads alarms/events/pending/probe metrics/workers in one repeatable-read snapshot and intentionally returns disconnected rather than falling back to a stale JSON file. SQLite/v1 retains file-backed monitor behavior; durable history is immutable and the UI hides the legacy clear action.
- Added direct-projection tests for monitor observation generation/validation, missing-observation preservation, pending delay lifecycle, conclusive clear, profile/mode suppression, write-time and stopped-stream scheduled retention, `probe.*` exclusion, repeatable-read interleaving, migration-003 up/down, PostgreSQL `/state.json` with an absent JSON file, actual `run_worker --once`, duplicate/reassignment/expiry/config/incarnation no-op behavior, and proxy identity. Independent review found and drove a repeatable-read snapshot fence, catalog filtering for pending reads, immutable-history UI handling, scheduled stopped-stream retention, and the required evidence/work-log reconciliation; no blocker/high implementation finding remained.
- Integration validation: fresh disposable PostgreSQL database `videosim_directfinal2` applied migrations 001/002/003 and, with NATS 2.11.17, ran `VIDEOSIM_TEST_POSTGRES_URL=... VIDEOSIM_TEST_NATS_URL=... /tmp/videosim-durable-venv/bin/python -m unittest tests.test_postgres_store tests.test_nats_publisher tests.test_worker_v2 -v`; all 51 tests passed. This includes direct transaction/read projection, repeatable-read concurrent interleaving, pending/delay/repeat/suppress/write-time and global stopped-stream retention, worker-v2 HTTP fencing, and transactional outbox/JetStream producer cases.
- Restore validation after migration 003: custom backup/empty-broker restore passed in 1 second with a 69,884-byte dump, migration identities 001/002/003, informational generation 1→2, 17 expired leases, 75 requeued non-dead outbox events, and semantic SHA-256 `c581a12595dc9d0d8a79e4d5db23f64fb314b44cc5ed1447ce8be0a6a2014258`. Semantic comparison passed across 25 feeds, 23 workers/leases, 49 reports, 35 results, current check/alarm state, 15 alarm events, `current_alarm_pending`, audit rows, 75 outbox events, and the empty consumer inbox; publishing the empty-broker restore into a fresh JetStream produced exactly 75 messages.
- Regression/packaging validation: `python3 -m unittest discover -s tests` passed 278 tests with 60 environment-gated skips; Python compilation, shell syntax, `git diff --check`, `npm run build-ui`, `npm audit --omit=dev`, `pip-audit -r requirements.txt`, and default/production Compose rendering passed; npm and Python dependency audits reported no known vulnerabilities. Live Docker image rebuild and media gates remain skipped for the previously recorded Docker Hub/network reason. Durable HTTP security-audit writes, inbox-deduplicated JetStream consumer/replay parity, HA/DR certification, capacity scheduling, and 1,000/5,000/10,000 admission remain open; no production-scale or HA claim follows.
- Added the selective durable HTTP audit cutover with migration `004_immutable_audit_outbox.sql`: PostgreSQL now writes durable proxy/worker/operator authentication or authorization denials best-effort, and commits successful persistent feed/profile mutations with their immutable audit/outbox record in one transaction. Persistent audit/outbox failure returns HTTP 503 without mutating the feed; allowed reads/workers and local runtime controls remain stdout-audited by policy.
- Added database enforcement for append-only audit rows (including truncate) and immutable outbox identity/content, while retaining only publisher delivery-state updates. Added `videosim_app`, `videosim_publisher`, and `videosim_pruner` non-owner service roles with passwords distinct from each other and the owner credential; pre-migration role convergence removes both membership directions, direct grants, and ownership, and post-migration grants restore the narrow policy on every deployment or restore. The pruner uses a scoped security-definer function rather than direct alarm-event access.
- Closed audit/lifecycle review findings: generated expectation changes now commit before local restart; durable deletion commits before best-effort detached cleanup; stale mode/alert updates cannot resurrect a concurrently deleted feed; explicit unknown stream targets never fall back to the selected stream; accepted query-bearing POSTs route by parsed path and never audit query secrets.
- Added integration/unit coverage for atomic create/update/alert/delete audit rows, failure rollback, denial identity/path sanitization, immutable database triggers, role convergence in both membership directions, owner-password reuse rejection, least-privilege app/publisher/pruner paths, cleanup failures, restore role convergence, and delete/mutation races. Independent review findings are resolved by the final post-fix review recorded with this task's validation artifacts.
- Integration validation: fresh PostgreSQL database `videosim_auditfinal7` applied migrations 001–004 through `postgres-runtime-role.sh prepare`, `videosim migrate`, and `postgres-runtime-role.sh grant`. Full `unittest discover -s tests` with PostgreSQL/NATS and all three runtime-role URLs passed 297 tests with 11 intentionally environment-gated live-media skips. Focused PostgreSQL/worker/NATS role and audit suites also passed.
- Restore/replay validation: a 104,220-byte custom-format backup of `videosim_auditfinal7` restored into a fresh target in `empty` broker mode. The restore converged a deliberately drifted incoming `videosim_app` membership, reapplied narrow runtime roles, expired 32 leases, requeued 114 non-dead outbox records, and the semantic verifier passed with SHA-256 `ea52fda702643f5639cb39489a52eedee1bc2c69f835e8eeb7cb61f92fdf4287`, including 21 audit rows and all 114 immutable outbox envelopes. A fresh JetStream replay through `videosim_publisher` published exactly 114 messages, including 44 `videosim.audit.v1` records.
- Regression/packaging validation: `npm run build-ui`, `npm audit --omit=dev`, `/tmp/videosim-durable-venv/bin/pip-audit -r requirements.txt`, Python compilation, shell syntax for backup/restore/runtime-role scripts, `git diff --check`, default and production Compose rendering, and migration status all passed. Live Docker image rebuild/media gates were not rerun because this slice does not change the media pipeline and the previously recorded Docker Hub/network limitation remains.
- Remaining F2 gate: inbox-deduplicated JetStream consumer/replay projection parity. The selective audit boundary is not an external/WORM archive, does not make denial persistence durable during a store outage, and does not certify HA, PITR/RPO/RTO, capacity, or 1,000/5,000/10,000 scale.
- Follow-up final review fixes: migration `005_feed_generation_fence.sql` retains per-feed configuration generations across physical deletion, so a stale PostgreSQL GUI process cannot overwrite a feed that was deleted and recreated with the same ID (the integration regression proves delete → recreate at version 3 → stale expectation/alert failure). The restore preflight now performs a read-only target-owner/superuser check and rejects a non-owner/runtime credential or owner-password reuse before any `pg_restore` command. Trusted worker/OIDC subjects are bounded to the durable-audit limit; oversized authenticated headers are rejected and still produce anonymous durable denial events rather than an audit insertion failure.
- Final integration validation: fresh PostgreSQL database `videosim_auditfinal9` applied migrations 001–005 through `postgres-runtime-role.sh prepare`, `videosim migrate`, and `postgres-runtime-role.sh grant`. Full `unittest discover -s tests` with PostgreSQL/NATS and all three runtime-role URLs passed 302 tests with 11 intentionally environment-gated live-media skips. This includes the cross-process delete/recreate ABA, oversized denial-audit, restore non-owner/no-`pg_restore`, migration-down, and runtime-role generation-privilege regressions.
- Final restore/replay validation: a 107,208-byte custom-format backup of `videosim_auditfinal9` restored into a fresh target in `empty` broker mode using the read-only owner preflight. The restore removed a deliberately drifted incoming `videosim_app` membership, reapplied narrow runtime roles, expired 31 leases, requeued 115 non-dead outbox records, and the semantic verifier passed with SHA-256 `ca893eb3edc3493acd196fccf0f8874ae50887b129ed8987436d0e709653603d`, including five migrations, 58 retained feed-generation rows, 23 audit rows, and all 115 immutable outbox envelopes. A fresh JetStream replay through `videosim_publisher` published exactly 115 messages, including 46 `videosim.audit.v1` records.
- Final packaging/static validation: `npm run build-ui`, `npm audit --omit=dev`, `/tmp/videosim-durable-venv/bin/pip-audit -r requirements.txt`, Python compilation, shell syntax for backup/restore/runtime-role scripts, `git diff --check`, default and production Compose rendering, and migration status for 001–005 passed. Live Docker image rebuild/media gates remain skipped because this slice does not change the media pipeline and the previously recorded Docker Hub/network limitation remains.
- 2026-07-12: Reviewed the distributed architecture against the current F0/F1/F2 implementation. Added durable PostgreSQL worker capacity admission via `capacity.maxStreams`, stable capacity-aware assignment with explicit `capacityShortfall`, capacity-preserving heartbeats, and the `worker --max-streams` CLI contract. Static admission is intentionally not a media-capacity claim; bounded concurrent execution, backpressure, HA, and 1,000-stream soak/failure evidence remain open.
- Validation: `python3 -m unittest tests.test_gui tests.test_worker tests.test_cli_video_feed` passed 108 tests; `python3 -m unittest tests.test_docs_contract` passed 4 tests; Python compilation, worker CLI help, and `git diff --check` passed.
- Skipped checks: PostgreSQL integration, Docker/live media, and production-like 1,000-stream capacity/failure gates were not run because the current environment has no configured durable service or certified workload infrastructure.
- 2026-07-12: Continued F3 toward the 1,000-stream target with bounded stream-level monitor concurrency. Added `--max-concurrent-checks`, preserved per-stream check ordering, propagated the bound through workers and capacity metadata, and kept the default at one.
- Validation: focused monitor/worker/CLI tests passed 66 tests; full `python3 -m unittest discover -s tests` passed 317 tests with 80 environment-gated skips; the 1,000-stream control-plane benchmark passed with `capacityCertified=false`; Python compilation and `git diff --check` passed.
- Skipped checks: PostgreSQL integration, Docker/live media, deadline/backpressure, and production-like 1,000-stream soak/failure gates remain pending.
- 2026-07-12: Continued F3 with opt-in per-stream probe budgets. Added `--stream-budget-seconds`, deferred remaining lower-priority checks after exhaustion, advertised the budget in worker capacity metadata, and preserved active alarms when timeout/error observations are inconclusive.
- Validation: focused monitor/worker/CLI tests passed 67 tests; an isolated task-only staged snapshot passed 308 tests with 77 environment-gated skips, and the current full worktree passed 318 with 80 skips; the 1,000-stream control-plane benchmark passed with `capacityCertified=false`; Python compilation and `git diff --check` passed.
- Skipped checks: PostgreSQL integration, Docker/live media, in-flight cancellation, cost-tier fairness, durable spool/backpressure/drain, and production-like 1,000-stream media soak/failure gates remain pending.
- 2026-07-12: Continued F3 by propagating each configured stream budget into built-in blocking probes. GStreamer receiver/frame probes, the SRT transport sampler, FFprobe, FFmpeg loudness, and DASH polling/socket waits now use the remaining budget and surface typed timeout observations; deadline context is isolated per worker thread.
- Validation: focused deadline/monitor/worker tests passed 61 tests; an isolated task-only staged snapshot passed 313 tests with 77 environment-gated skips, and the current full worktree passed 323 with 80 skips; the 1,000-stream control-plane benchmark passed with `capacityCertified=false`; Python compilation and `git diff --check` passed.
- Skipped checks: PostgreSQL integration, Docker/live media, arbitrary-checker and trickle-resistant HTTP cancellation, cost-tier fairness, durable spool/backpressure/drain, and production-like 1,000-stream media soak/failure gates remain pending.
- 2026-07-12: Continued F3 with validation-first worker scheduling. Concurrent workers now use one bounded pool in two phases, completing validation for every assigned stream before TR-101/frame-rate/loudness work; phase two reuses the original config, validation result, deadline, alarm/pending state, observations, and metrics.
- Validation: focused monitor/worker tests passed 44 tests; an isolated task-only staged snapshot passed 314 tests with 77 environment-gated skips, and the current full worktree passed 324 with 80 skips; the 1,000-stream control-plane benchmark passed with `capacityCertified=false`; Python compilation and `git diff --check` passed.
- Skipped checks: PostgreSQL integration, Docker/live media, black/frozen validation cost isolation, protocol/tenant cost tokens, durable spool/backpressure/drain, slow-stream-storm freshness, and production-like 1,000-stream media soak/failure gates remain pending.
- 2026-07-12: Continued F3 with graceful worker drain and a repeatable startup-validation workflow. SIGINT/SIGTERM now completes the current report before stopping heartbeats and posting an incarnation-fenced drain; PostgreSQL marks matching live leases draining for immediate higher-epoch reassignment and rejects late same-incarnation reactivation. The isolated Compose workflow boots app/worker services, verifies readiness/registration through HTTP, creates/starts/validates/stops a normal SRT feed, and captures JSON, process state, and Docker logs. README, runbook, acceptance matrix, test plan/gaps, and distributed progress now mark the automated API gate and manual Google Chrome path.
- Validation: `VIDEOSIM_STARTUP_INTEGRATION=1 python3 -m unittest tests.test_startup_workflow -v` passed the built-image workflow; the final cached-image rerun with Docker-log content/traceback checks passed in 12.2 seconds. An isolated task-only staged snapshot passed 320 tests with 80 environment-gated skips, then passed its own rebuilt Docker startup/API/media/log gate in 55.0 seconds. The current full worktree passed 330 tests with 83 skips. Focused docs/worker/API/GUI/startup tests passed 99 tests with one expected startup skip. Python compilation, default Compose rendering, and `git diff --check` passed. The 1,000-stream/10-worker control-plane benchmark passed with 100 assignments per worker and `capacityCertified=false`.
- Skipped checks: PostgreSQL drain/reassignment integration was not run because `VIDEOSIM_TEST_POSTGRES_URL` is not configured. The startup smoke is one local normal feed; production PostgreSQL/NATS startup, encrypted report spool/backpressure, hard-kill recovery timing, slow-stream storms, and production-like 1,000-stream media soak/failure evidence remain pending.
- 2026-07-12: Continued F3 with opt-in staggered deep-check cadence. `--deep-check-interval-seconds` keeps critical validation on every worker cycle while scheduling TR-101, frame-rate, and loudness work at stable per-stream offsets. Deferred deep phases emit one bounded skipped metric and no health observation, preserve active alarms, and retain only current-assignment schedule state. Worker reports omit that internal schedule map, capacity registration advertises the cadence, and the shared API boundary validates all advertised probe controls as bounded/finite values.
- Validation: focused monitor/worker/GUI/CLI tests passed 144 tests. An isolated task-only staged snapshot passed 324 tests with 80 environment-gated skips; the current full worktree passed 334 with 83 skips. The deterministic 1,000-stream cadence test populated every five-second slice of a 60-second interval with no slice above 12.5% of streams. Python compilation, worker CLI help, default Compose rendering, docs contract, and `git diff --check` passed. The 1,000-stream/10-worker control-plane benchmark passed with 100 assignments per worker and `capacityCertified=false`.
- Skipped checks: the configured cadence is not dynamic saturation feedback. Protocol/tenant cost tokens, encrypted durable report spooling, pressure/recovery telemetry, slow-stream media storms, PostgreSQL integration, and production-like 1,000-stream soak/failure evidence remain pending.
- 2026-07-12: Continued F3 with opt-in aggregate batch-pressure shedding. `--batch-budget-seconds` completes critical validation for every assignment, then defers only due TR-101/frame-rate/loudness work when validation consumed the cycle budget. Deferral remains inconclusive, preserves alarms, advances configured stable cadence offsets, and is advertised in validated worker capacity metadata. This is a soft deep-phase guard, not a validation deadline, durable queue, or capacity claim.
- Validation: focused monitor/worker/GUI/CLI tests passed 147 tests. An isolated task-only staged snapshot passed 326 tests with 80 environment-gated skips; the current full worktree passed 336 tests with 83 skips. Python compilation, worker CLI help, default Compose rendering, docs contract, staged/current diff checks, and the 1,000-stream/10-worker control-plane benchmark passed; the benchmark assigned 100 streams per worker with `capacityCertified=false` and 39.294/39.585/39.611 ms p50/p95/p99 cycle duration.
- Startup validation: the cached-image Docker workflow passed its API readiness, worker registration, normal SRT video/audio/captions, stopped/unreachable, process-state, and Docker-log traceback checks in 12.077 seconds. A fresh image rebuild was attempted first but stopped after `docker-credential-desktop get` remained hung for about three minutes; the cached-image result is not fresh-image evidence.
- Skipped checks: the blocked Docker rebuild, PostgreSQL integration, validation-phase saturation/kill behavior, pressure/recovery telemetry, protocol/tenant fairness, encrypted durable report spooling, full queue backpressure, slow-stream media storms, and production-like 1,000-stream soak/failure evidence remain pending.
- 2026-07-12: Continued F3 with an authenticated-encrypted worker-v2 report spool. Workers now encrypt and fsync exact fenced report payloads before send, replay old entries before registering a new incarnation, remove only accepted or explicitly stale-fenced reports, enforce an operator byte quota, and pause new probes while delivery is blocked. Production Compose persists the spool separately from its mode-0600 Fernet key and emits bounded blocked/recovered transition logs.
- Validation: focused spool/worker/CLI tests passed 50 tests. A clean task-only staged snapshot passed 334 tests with 80 environment-gated skips; the current full worktree passed 344 tests with 83 skips. A clean virtual environment installed the exact requirements, `pip check` passed, and `pip-audit -r requirements.txt` reported no known vulnerabilities. Python compilation, worker CLI help, default/production Compose rendering, shell syntax, docs contract, and staged/current diff checks passed.
- Startup/image validation: after the desktop credential helper hung, an isolated empty Docker client config bypassed it and the fresh staged-image startup/API/normal-SRT/media/log gate passed in 16.480 seconds. The built Linux worker image reported `cryptography==49.0.0`, and a bind-mounted generated mode-0600 key plus mounted spool directory initialized successfully inside that image.
- Foundation benchmark: the staged 1,000-stream/10-worker control-plane benchmark passed 30 report acceptances with 100 assignments per worker, `capacityCertified=false`, and 37.987/38.301/38.329 ms p50/p95/p99 cycle duration. It remains non-media evidence.
- Skipped checks: real control-plane outage/restart and disk-fill recovery, corrupted-entry repair, automatic key rotation, fleet spool telemetry, PostgreSQL integration, full probe-queue backpressure/fairness, HA/failover, slow-stream media storms, and production-like 1,000-stream soak/failure evidence remain pending.
- 2026-07-12: Continued F3 with durable protocol-aware admission tokens. Workers can advertise `maxSrtStreams` and `maxDashStreams`; the shared PostgreSQL scheduler now enforces matching protocol limits together with `maxStreams`, preserves compatibility round-robin for unconfigured workers, and reports excess mixed demand through the existing capacity shortfall.
- Validation: focused GUI/worker/CLI tests passed 120 tests. An isolated task-only staged snapshot passed 336 tests with 80 environment-gated skips; the current full worktree passed 346 tests with 83 skips. The new deterministic target-shaped test assigned a 500-SRT/500-DASH workload across ten workers at exactly 50/50 and 100 total per worker, with zero shortfall. Python compilation, worker CLI help, default/production Compose rendering, docs contract, and repository staged/current diff checks passed.
- Startup and benchmark validation: the fresh staged-image startup/API/normal-SRT/media/Docker-log gate passed in 13.372 seconds using the isolated Docker client config. The separate staged 1,000-stream/10-worker control-plane benchmark passed 30 report acceptances with 100 assignments per worker, `capacityCertified=false`, and 38.718/38.879/38.893 ms p50/p95/p99 cycle duration.
- Skipped checks: protocol limits are operator-provided stream counts, not measured weighted check costs or media-capacity evidence. Tenant fairness, separate protocol/check execution pools, full probe-queue backpressure, PostgreSQL integration, HA/failover, slow-stream storms, and production-like 1,000-stream soak/failure evidence remain pending.
- 2026-07-12: Continued F3 with bounded worker probe admission. Validation and deep checks now enter the executor in concurrency-sized windows; aggregate budget exhaustion stops new validation starts, emits inconclusive skipped observations without false-clearing alarms, and rotates deferred streams to the front of the next cycle. Worker-local cursor state is excluded from reports.
- Validation: focused monitor/worker/CLI/GUI coverage passed 156 tests; an isolated task-only staged snapshot passed 338 tests with 80 environment-gated skips, and the full current worktree passed 348 tests with 83 skips. A deterministic 1,000-assignment test admitted only the configured eight-probe window, accounted for 992 deferred validations, and advanced the fairness cursor. Python compilation, docs contracts, default/production Compose rendering, and diff checks passed.
- Startup and benchmark validation: the staged-image Compose startup/API/normal-SRT/media/Docker-log integration gate passed in 13.084 seconds. The staged 1,000-stream/10-worker control-plane benchmark passed with 44.792/45.816/45.907 ms p50/p95/p99 cycle duration and still declares no media-capacity certification.
- Skipped checks: in-flight probes remain bounded by per-stream deadlines rather than a hard aggregate wall clock. Durable fleet queueing, arbitrary-checker/trickling-HTTP cancellation, weighted check-cost/tenant fairness, worker pressure telemetry, slow-stream storms, HA/failover, and production-like 24-hour 1,000-stream media evidence remain pending.
- 2026-07-12: Continued F3 with independent deep-check concurrency. Workers can advertise and configure `maxConcurrentDeepChecks`/`--max-concurrent-deep-checks`; validation and TR-101/frame-rate/loudness windows now use separate static limits on the existing bounded executor. Also removed an order-sensitive assertion from the concurrent rotation test.
- Validation: focused monitor/worker/GUI/CLI coverage passed 157 tests. An isolated task-only staged snapshot passed 339 tests with 80 environment-gated skips, and the full current worktree passed 349 tests with 83 skips. The concurrency barrier proved validation peak 2 and deep-check peak 1; Python compilation, worker help, docs contracts, default/production Compose rendering, and diff checks passed.
- Startup and benchmark validation: the staged-image Compose startup/API/normal-SRT/media/Docker-log gate passed in 13.195 seconds. The staged 1,000-stream/10-worker control-plane benchmark passed with 43.128/43.344/43.363 ms p50/p95/p99 cycle duration and still declares no media-capacity certification.
- Skipped checks: validation/deep limits are operator settings on one executor, not measured weighted-cost pools or scheduler admission evidence. Tenant fairness, durable fleet queueing, pressure telemetry, slow-stream storms, HA/failover, and production-like 24-hour 1,000-stream media evidence remain pending.
- 2026-07-12: Continued F3 with bounded latest worker-pressure visibility. Independent heartbeats now publish schema-validated assigned/cycle/validation-deferral/deep-deferral/batch-duration and encrypted-spool occupancy fields; PostgreSQL operator `/state.json` exposes the latest snapshot without adding a history table or unbounded labels.
- Validation: focused worker/GUI/monitor/PostgreSQL coverage passed 176 tests with 43 environment-gated skips; the current full worktree passed 350 tests with 83 skips. An isolated task-only staged snapshot passed 340 tests with 80 skips, Python compilation, four docs-contract tests, default/production Compose rendering, and staged diff checks. The staged 1,000-stream/10-worker control-plane benchmark passed with 40.831/43.023/43.217 ms p50/p95/p99 cycle duration and explicitly remains non-media capacity evidence.
- Startup validation: the staged-image Compose workflow booted the app and worker, validated normal SRT video/audio/captions plus stopped/unreachable behavior through the API, and checked process state and Docker logs in 12.967 seconds. The documented Google Chrome path remains available for human-visible validation.
- Skipped checks: PostgreSQL projection integration remains environment-gated because `VIDEOSIM_TEST_POSTGRES_URL` is not configured. Pressure history, alert policies, recovery-SLO evidence, weighted protocol/check cost, tenant fairness, slow-stream storms, HA/failover, and production-like 24-hour 1,000-stream media evidence remain pending.
- 2026-07-12: Continued F3 with pressure-aware durable assignment. Workers reporting `spoolBlocked=true` remain visible but receive no assignments; healthy workers can receive replacement authority, and insufficient/all-blocked fleets return explicit `capacityShortfall`. A deterministic modeled test places a 1,000-stream target plus 30% headroom across nine surviving workers without over-admitting total or protocol tokens.
- Validation: focused GUI/worker/benchmark/docs coverage passed 108 tests. The current full worktree passed 353 tests with 83 environment-gated skips; an isolated task-only staged snapshot passed 343 tests with 80 skips, Python compilation, four docs-contract tests, default/production Compose rendering, and staged diff checks. The staged 1,000-stream/10-worker control-plane benchmark passed with 42.509/42.818/42.845 ms p50/p95/p99 cycle duration and explicitly remains non-media capacity evidence.
- Startup validation: the staged-image Compose workflow booted the app and worker, validated normal SRT video/audio/captions plus stopped/unreachable behavior through the API, and checked process state and Docker logs in 13.207 seconds. The manual Google Chrome path remains documented.
- Skipped checks: the pressure-aware scheduler was not exercised through the environment-gated PostgreSQL HTTP integration because `VIDEOSIM_TEST_POSTGRES_URL` is not configured. The 1,300-item test is placement math only; pressure hysteresis/history/alerts, durable fleet queueing, recovery and slow-stream storms, measured worker/media headroom, tenant fairness, HA/failover, and the production-like 24-hour 1,000-stream admission run remain pending.
- 2026-07-12: Started the F5 evidence foundation with versioned JSON workload/evidence/policy contracts and `python -m videosim capacity-check`. The verifier requires all ten admission criteria and baseline artifact classes, validates 24-hour/30%-headroom/one-failure-domain declarations plus surviving total/SRT/DASH tokens, and rejects dirty/failed/skipped runs, path escape, missing files, and SHA-256 drift. Added the strict `scale/policies/f5-1000.json` policy; the command verifies evidence but does not generate a workload or capacity claim.
- Validation: seven focused scale-evidence tests passed complete-bundle/real-policy, CLI JSON, tamper, missing-kind, path-escape, weak-policy, dirty/failed/skipped/duration, mix/headroom, and survivor-capacity cases. Focused CLI/docs coverage passed 35 tests. An isolated task-only staged snapshot passed 350 tests with 80 environment-gated skips; the current full worktree passed 360 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, and staged diff checks passed.
- Startup and benchmark validation: the staged-image Compose workflow booted the app and worker, validated normal SRT video/audio/captions plus stopped/unreachable behavior through the API, and checked process state and Docker logs in 13.292 seconds. The staged 1,000-stream/10-worker control-plane benchmark passed with 38.256/38.729/38.771 ms p50/p95/p99 cycle duration and still declares no media-capacity certification.
- Skipped checks: no production-like F5 workload or evidence bundle exists, so `capacity-check` has not admitted 1,000 streams. The load driver/fixtures, representative media/resource measurement, 24-hour run, failure storms, real 30% headroom after domain loss, dashboards/alerts, security/restore/rollback evidence, tenant fairness, HA control plane/storage/broker, and human approval remain pending.
- 2026-07-12: Continued F0/F3 with worker-batch resource instrumentation on the existing bounded heartbeat pressure snapshot. Completed batches report worker-plus-reaped-child CPU delta, cumulative worker and child peak RSS, and Linux post-batch open-file-descriptor count; the shared capacity boundary rejects negative/non-finite/wrong-type values.
- Validation: focused worker/GUI/docs coverage passed 105 tests. An isolated task-only staged snapshot passed 352 tests with 80 environment-gated skips; the current full worktree passed 362 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, and staged/current diff checks passed.
- Startup and benchmark validation: the staged-image Compose workflow booted the app and worker, validated normal SRT video/audio/captions plus stopped/unreachable behavior through the API, and checked process state and Docker logs in 13.245 seconds. The staged 1,000-stream/10-worker control-plane benchmark passed with 38.380/38.402/38.404 ms p50/p95/p99 cycle duration and still declares no media-capacity certification.
- Skipped checks: resource snapshots have no history and are not an aggregate peak across concurrent children; descriptor measurement is Linux-only, and media-byte/socket accounting remains absent. Representative per-check baselines, saturation curves, PostgreSQL HTTP projection integration, and production-like 1,000-stream resource/headroom evidence remain pending.
- 2026-07-12: Continued F0 with `python -m videosim worker-benchmark`. The command hashes an exported GUI-state scenario, runs the existing real monitor/media-probe path with worker concurrency/cadence/budget controls, and emits cycle/CPU percentiles, outcomes, worker/child peak RSS, Linux descriptors, stream/check counts, and explicit `capacityCertified=false` limitations.
- Validation: focused worker-benchmark/CLI/docs coverage passed 32 tests. An isolated task-only staged snapshot passed 355 tests with 80 environment-gated skips; the current full worktree passed 365 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, and staged/current diff checks passed.
- Live media and startup validation: inside the staged Linux image, a one-stream normal SRT scenario completed four checks in 1,555.073 ms with 269.619 ms CPU, 39,395,328 worker peak RSS bytes, 129,323,008 child peak RSS bytes, and four descriptors; outcomes were one success, one issue, and two intentional external-feed skips, while the harness passed coverage and retained `capacityCertified=false`. The staged Compose startup/API/normal-SRT/media/Docker-log gate passed in 13.340 seconds.
- Foundation benchmark and skipped checks: the staged 1,000-stream/10-worker control-plane benchmark passed with 38.484/39.448/39.533 ms p50/p95/p99 cycle duration and remains non-media evidence. No representative multi-stream scenario, per-worker saturation curve, fixture fleet, distributed load/failure run, media-byte accounting, 24-hour soak, or production-like 1,000-stream headroom evidence exists yet.
- 2026-07-12: Added a manifest-driven DASH endpoint fixture fleet using the existing feed generator and a standard-library HTTP fault service. The checked-in matrix exports a four-stream worker scenario with healthy, 30-second slow, `503` dead, and malformed MPD endpoints; the CLI supports a container-network advertised-host override. SRT transport fixtures remain pending.
- Validation: focused fixture/worker/docs/CLI coverage passed 36 tests, and focused fixture/validator coverage passed 12 tests. An isolated task-only staged snapshot passed 359 tests with 80 environment-gated skips; the current mixed worktree passed 369 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, staged/current diff checks, and the staged 1,000-stream/10-worker control-plane benchmark passed; the benchmark still declares no media-capacity certification.
- Live fixture and startup validation: the exact staged Linux healthy endpoint passed independent validation with reachability plus video, audio, and captions present. The four-stream matrix completed 16 real probe checks in 5,022.755 ms with 51.412 ms CPU, 39,763,968 worker peak RSS bytes, zero reaped-child peak RSS, and four descriptors. Outcomes were one success, one issue, and 14 bounded timeouts because the slow endpoint consumed other deep-check windows; the harness passed coverage and retained `capacityCertified=false`. The Docker startup workflow passed API readiness, worker registration, normal SRT video/audio/captions validation, feed stop, and log capture in 7.4 seconds using existing repository images.
- Skipped checks: a fresh Docker rebuild was stopped after Docker Hub base-image metadata resolution stalled for about three minutes before container startup; the no-build workflow passed. Equivalent SRT fixtures, generated/external source cross-product, slow-stream-storm scheduling, long-run fixture reliability, distributed load/failure, and production-like 24-hour 1,000-stream headroom evidence remain open.
- 2026-07-12: Extended the manifest-driven fixture fleet with deterministic SRT transport behavior on four consecutive ports. The matrix runs the existing normal MPEG-TS generator, a 30-second stalled listener, an unbound dead port, and a paced non-MPEG-TS listener; it exports the same four-stream benchmark-state contract and cleans all children on exit.
- Validation: seven focused fixture tests and 11 fixture/docs-contract tests passed. An isolated task-only staged snapshot passed 362 tests with 80 environment-gated skips; the current mixed worktree passed 372 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, staged/current diff checks, and the staged 1,000-stream/10-worker control-plane benchmark passed; the benchmark remains non-media evidence.
- Live SRT and startup validation: the exact staged Linux healthy endpoint passed reachability/video/audio/captions validation and yielded 110,168 valid TS bytes in a one-second sample. Slow and dead yielded zero bytes; malformed yielded 1,692 bytes with sync-loss and sync-byte errors. The four-stream worker benchmark completed 16 checks in 5,015.014 ms with 969.839 ms CPU, 38,944,768 worker peak RSS bytes, 128,753,664 child peak RSS bytes, four descriptors, one success, and 15 bounded timeouts while retaining `capacityCertified=false`; every fixture process remained running. The no-build Docker startup workflow passed API readiness, worker registration, normal SRT validation, feed stop, and log capture in 7.47 seconds.
- Skipped checks: a fresh image rebuild was not retried after the Docker Hub metadata stall recorded in the preceding slice. The generated/external and SRT/DASH source cross-product, sustained slow/dead/malformed storm scheduling, long-run fixture reliability, distributed failure/headroom, HA, and production-like 24-hour 1,000-stream admission evidence remain open.
- 2026-07-12: Added `fixture-scenario` and the seeded `scale/fixtures/mixed-1000.json` manifest. The composer validates and hashes both protocol fixture states, creates 1,000 unique logical IDs with an exact 500-SRT/500-DASH and 800/100/50/50 healthy/slow/dead/malformed allocation, and declares that all logical streams reuse eight physical endpoints.
- Validation: 14 focused composer/fixture/docs-contract tests passed. An isolated task-only staged snapshot passed 365 tests with 80 environment-gated skips; the current mixed worktree passed 375 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, staged/current diff checks, and the staged 1,000-stream/10-worker control-plane benchmark passed; the control-plane benchmark measured 39.960/40.008/40.012 ms p50/p95/p99 and remains non-media evidence.
- Live mixed-worker validation: exact staged Linux SRT and DASH fleets produced the 1,000-stream state, then one worker with eight validation tokens, two deep tokens, 250 ms per-stream budgets, and a 100 ms aggregate budget completed a 667.609 ms cycle. It reported 2,000 checks, eight bounded timeouts, 1,992 explicit skips, 954.862 ms worker-plus-child CPU, 69,918,720 worker peak RSS bytes, 128,258,048 child peak RSS bytes, and four descriptors; both fleets remained running and `capacityCertified=false`.
- Startup and skipped checks: the no-build Docker workflow passed API readiness, worker registration, normal SRT video/audio/captions validation, feed stop, and log capture in 8.45 seconds. A fresh image rebuild was not retried after the recorded Docker Hub metadata stall. Eight shared endpoints do not prove independent stream load, full-cycle freshness, source cross-product, sustained storms, distributed headroom/failure, HA, or production-like 24-hour 1,000-stream admission.
- 2026-07-12: Extended `worker-benchmark` with benchmark-local unique validation-start coverage and opt-in `--require-full-validation-coverage`. It derives attempted IDs from existing per-cycle probe metrics, reports coverage percentage/cycle-to-full-coverage, and fails closed when required coverage is incomplete without adding ID sets to worker heartbeats or reports.
- Validation: 48 focused worker-benchmark/monitor/composer/docs tests passed. An isolated task-only staged snapshot passed 366 tests with 80 environment-gated skips; the current mixed worktree passed 376 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, staged/current diff checks, and the staged 1,000-stream/10-worker control-plane benchmark passed; the control-plane benchmark measured 40.064/40.519/40.560 ms p50/p95/p99 and remains non-media evidence.
- Sustained rotation validation: exact staged Linux SRT/DASH fixtures ran 125 measured cycles with eight validation tokens, 30 ms stream budgets, and a 1 ms aggregate budget. The strict gate passed with all 1,000 logical streams attempted and full coverage on cycle 125; cycle p50/p95/p99 was 580.872/595.098/598.363 ms, CPU p50/p95/p99 was 568.914/582.758/586.263 ms, worker and child peak RSS were each 74,489,856 bytes, and descriptors remained four. Outcomes were 157 success, 25 issue, 818 bounded timeout, and 249,000 explicit skips; `capacityCertified=false` remained enforced.
- Startup and skipped checks: the no-build Docker startup/API/normal-SRT/media/log workflow passed in 8.56 seconds. The run proves starvation-free validation starts under one shared-endpoint pressure configuration, not approved detection freshness, independent stream throughput, a saturation curve, tenant fairness, failure-domain recovery, HA, or production-like 24-hour 1,000-stream admission.
- 2026-07-12: Extended `control-plane-benchmark` with opt-in process-local worker removal after warmup. It requires complete unique survivor assignments, rejects each removed worker's stale fenced report, measures failover calculation time, and reports minimum versus excess assignment movement so coverage cannot hide unstable placement.
- Validation: 87 focused benchmark/GUI/docs tests passed. An isolated task-only staged snapshot passed 367 tests with 80 environment-gated skips; the restored mixed worktree passed 377 tests with 83 skips. Python compilation, docs contracts, default/production Compose rendering, and staged/current diff checks passed.
- Failure benchmark: the exact staged 1,300-stream/ten-worker run removed one worker, assigned all streams across nine survivors at 144-145 each, accepted 27 measured reports, rejected the stale failed-worker report, and calculated failover in 23.809 ms. Measured cycle p50/p95/p99 was 45.542/46.119/46.171 ms. It moved 1,174 assignments versus a 130 minimum, exposing 1,044 excess moves; `mediaProbesExecuted=false` and `capacityCertified=false` remained enforced.
- Startup, staging, and skipped checks: the no-build Docker startup/API/normal-SRT/media/log workflow passed in 8.45 seconds. This commit also removes three replay-ledger documentation rows accidentally included in the preceding task commit and restores them as unstaged user changes. Durable PostgreSQL lease expiry/partition recovery, stable placement, real failure domains, media load, HA, and 24-hour 1,000-stream admission remain open.
- 2026-07-12: Added sticky balanced durable placement. Assignment polls retain fresh offered/active lease owners up to the current total/SRT/DASH target, move only unavailable ownership after worker loss, and still perform the minimum rebalance needed when a worker joins. The modeled 1,300-stream/ten-worker loss now moves exactly the failed worker's 130 streams instead of the 1,174 moves measured by the process-local stateless benchmark.
- Validation: the task-only staged snapshot passed 370 tests with 81 environment-gated skips, four documentation-contract tests, Python compilation, default and production Compose rendering, and staged diff checks. The mixed worktree passed 380 tests with 84 skips. Three fresh-PostgreSQL staged tests passed stale-owner exclusion, two-worker join/handoff, and concurrent unique assignment. A broader mixed 68-test PostgreSQL/worker-v2 run had 63 passes, four skips, and one shared-default-tenant ordering failure in an unrelated pressure assertion; that assertion passed alone on a fresh database.
- Startup and skipped checks: the documented no-build workflow passed in 8.62 seconds through API readiness, worker registration, feed create/start, normal SRT video/audio/captions validation, and stop. Captured Compose state showed app and worker up; Docker logs had no traceback, fatal error, or validation failure, though GStreamer emitted its existing caption-framerate negotiation warning before validation passed. Lease-TTL expiry, network partition, real process/failure-domain loss, replicated scheduler leadership, tenant fairness, independent media saturation, HA, and the 24-hour 1,000-stream admission gate remain open.
- 2026-07-12: Added real-time hard-kill recovery evidence for the existing PostgreSQL worker-v2 path. With a one-second test TTL, a pre-expiry survivor poll retains only its unchanged lease, a heartbeating survivor takes the failed stream only after worker/lease expiry, the replacement epoch advances, and the stale failed-owner report receives `409`.
- Validation: the exact task-only staged snapshot passed 371 tests with 82 environment-gated skips, four documentation-contract tests, Python compilation, default and production Compose rendering, and staged diff checks. The complete current worker-v2 suite passed 25 tests on a fresh PostgreSQL database; the exact staged TTL test passed in 1.958 seconds.
- Startup and skipped checks: the no-build startup workflow passed API readiness, worker registration, normal SRT video/audio/captions validation, stop, process capture, and Docker-log review in 8.57 seconds with no traceback, fatal error, or validation failure. The default 60-second TTL plus assignment-poll recovery bound, network partitions, actual process/failure-domain termination, fault storms, replicated leadership, and production recovery SLO remain unmeasured.
- 2026-07-12: Extended `worker-benchmark` with opt-in repeated validation-cadence enforcement. `--max-validation-gap-cycles N` requires every running stream to start validation at least twice and fails when the initial, repeat, or trailing service window exceeds `N`; default behavior remains unchanged. The report now exports minimum attempts and maximum cycle gap, and a regression proves that two early attempts followed by starvation fail.
- Validation: 34 focused worker-benchmark/CLI/docs tests passed. The exact task-only staged snapshot passed 372 tests with 82 environment-gated skips, four documentation-contract tests, Python compilation, default and production Compose rendering, and staged diff checks.
- Sustained mixed-fixture validation: exact staged Linux SRT/DASH fleets remained running while one worker completed 250 cycles over 1,000 logical streams with eight validation tokens, 30 ms stream budgets, and a 1 ms aggregate budget. The fail-closed gate passed with all streams attempted, at least two starts per stream, full first coverage in 125 cycles, and a 125-cycle maximum initial/repeat/trailing gap. Cycle p50/p95/p99 was 584.163/602.220/615.183 ms; CPU p50/p95/p99 was 572.244/595.922/606.863 ms; worker and child peak RSS were each 74,215,424 bytes; descriptors remained four. Outcomes were 468 success, 50 issue, 1,482 bounded timeout, and 498,000 explicit skips.
- Startup and skipped checks: the no-build API/normal-SRT/media/Docker-log workflow passed in 8.48 seconds. The repeated run uses 1,000 logical assignments over eight shared endpoints and cycle-based pressure controls; it does not establish an approved wall-time freshness SLO, independent media throughput, a saturation curve, failure-domain recovery, tenant fairness, HA, 24-hour soak, or F5 capacity admission, and `capacityCertified=false` remained enforced.
- 2026-07-12: Batched the durable lease lifecycle without changing its fences. One worker assignment poll now reconciles all offer/renew rows in one ordered transaction, and one acknowledgement request validates its full array before committing it atomically. The single-lease methods delegate to the batch methods. Missing feeds, stale tuples, and duplicate IDs fail the whole batch instead of leaving partial authority.
- Validation: the exact task-only staged snapshot passed 373 tests with 83 environment-gated skips, four documentation-contract tests, Python compilation, default and production Compose rendering, and staged diff checks. On fresh PostgreSQL, 27 batch-store/worker-v2 integration tests passed, including concurrent assignment, config fencing, hard-kill TTL recovery, and rollback after a late invalid tuple.
- Durable lifecycle measurement: exact staged code created and retained 1,300 active leases across ten workers at 130 streams each. Ten offer transactions took 1,215.837 ms, ten acknowledgement transactions took 845.810 ms, and five ten-transaction renewal sweeps measured 1,259.501/1,231.931/1,240.098/1,242.672/1,253.254 ms with a 1,242.672 ms median. The no-build startup API/normal-SRT/media/Docker-log workflow passed in 8.47 seconds. This local sequential DB run is not capacity certification; per-stream SQL, concurrent lock waits, replicated scheduler leadership, production PostgreSQL saturation, and F5 admission remain open.
- 2026-07-13: Added transaction-scoped PostgreSQL scheduler leadership. Each durable assignment decision now takes one tenant advisory lock and uses that same transaction for worker/owner reads, ordered offer/renew, and revocation. Concurrent replicas fail retryably; loss of the lock-holding database session rolls back all lease changes before a second store takes leadership.
- Validation: the exact task-only worktree passed 374 tests with 84 environment-gated skips, 28 fresh-PostgreSQL store/worker-v2 tests, four documentation-contract tests, Python compilation, default and production Compose rendering, and diff checks. The forced-session-loss test proved lock exclusion, rollback of an offered lease, and immediate second-store takeover; all 25 worker-v2 HTTP tests remained green.
- Scheduler/startup evidence and skipped checks: exact task-only code retained 1,300 unique assignments across ten workers at 130 each. Five complete scheduler sweeps measured 1,433.046/1,452.162/1,364.759/1,562.471/1,502.892 ms with a 1,452.162 ms median and `capacityCertified=false`. The no-build startup/API/normal-SRT/media/log workflow passed in 8.468 seconds; both containers were up, and logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate negotiation warning. This is local single-PostgreSQL and API-path evidence, not a deployed three-replica control plane, persistent leader, HA DB/broker/zone failover, independent media saturation, or 24-hour F5 admission.
- 2026-07-13: Extended the fixture-scenario composer to consume multiple fixture URLs per protocol/behavior, rotate deterministically across them, and derive per-stream plus scenario-level endpoint sharing from the composed result. Legacy four-endpoint fixture states remain compatible, and endpoint URL uniqueness still carries an explicit no-capacity limitation.
- Validation: the exact task-only worktree passed 375 tests with 84 environment-gated skips, including 13 focused fixture-scenario/worker-benchmark/docs checks, Python compilation, default and production Compose rendering, and diff checks. A 16-stream test composed two URLs for every SRT/DASH behavior into 16 distinct endpoints with no false sharing flags.
- Startup and skipped checks: the no-build startup workflow passed API readiness, worker registration, normal SRT video/audio/captions validation, stop, process capture, and Docker-log review in 7.435 seconds; both containers were up and logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate warning. The checked-in 1,000-stream states still use eight shared services; no independent fixture fleet, media saturation curve, PostgreSQL integration, or capacity admission is claimed by this composer-only slice.
- 2026-07-13: Added bounded per-behavior endpoint counts to fixture-fleet manifests and checked in exact 500-URL SRT and DASH mixes. Expanded SRT state maps each URL to its own listener port and starts one process for each non-dead endpoint; expanded DASH paths remain distinct URLs served by the existing shared generator/origin.
- Validation: the exact task-only worktree passed 377 tests with 84 environment-gated skips and 17 focused fixture/composer/docs checks. Manifest tests proved 500 unique URLs per protocol with the 400/50/25/25 behavior mix. A live six-endpoint SRT smoke bound two healthy, one slow, and two malformed UDP listeners while leaving the declared dead port unbound; both healthy feeds proved reachability/video/audio/captions, and dead plus malformed validation failed as expected.
- Startup and skipped checks: the marked no-build integration passed in 12.150 seconds, and the final retained startup workflow passed API readiness, worker registration, normal SRT video/audio/captions validation, stop, process capture, and Docker-log review in 8.459 seconds. Both containers were up; logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate warning. The 475-live-process SRT fleet and full 1,000-URL composition were not run, DASH still shares one origin/generator, and independent media saturation, HA, 24-hour soak, and F5 admission remain open.
- 2026-07-13: Replaced one-encoder-per-healthy-SRT-fixture scaling with one caption-capable encoder, one local UDP multicast distributor, and a lightweight SRT relay per healthy listener. Slow and malformed listeners remain isolated processes, dead ports remain unbound, legacy four-endpoint commands remain unchanged, and expanded manifests reserve two internal transport ports.
- Live 1,000-URL evidence: the exact task-only Linux source ran the checked-in 500-SRT and 500-DASH fleets simultaneously and composed 1,000 unique URLs with exact 500/500 protocol and 800/100/50/50 behavior counts. All 400 unique healthy SRT sockets passed reachability/video/audio/captions validation, all 400 healthy DASH URLs returned MPDs, one DASH URL passed full media validation, and representative slow/dead/malformed endpoints produced the expected failures. The 500-SRT container remained up for seven minutes and measured 2.249 GiB, 3,830 PIDs/threads, and 320.30% CPU after the sweep; the DASH container measured 73.61 MiB and 20 PIDs/threads.
- Ramp comparison and skipped checks: the prior encoder-per-port topology measured 796.3 MiB/378 PIDs at 20 endpoints and 2.986 GiB/1,441 PIDs at 80; the multicast-relay topology measured 159.8 MiB/200 PIDs and 409.5 MiB/707 PIDs respectively. This is sequential one-pass smoke evidence, not a saturation ceiling or F5 admission. Healthy streams still share one source per protocol; short reconnect cadence, concurrent freshness, source cross-product, failure storms/domains, 30% headroom, tenant fairness, HA, and the 24-hour soak remain open.
- Validation and startup: the exact task-only worktree passed 378 tests with 84 environment-gated skips, including 18 focused fixture/composer/docs tests, Python compilation, default and production Compose rendering, and diff checks. The no-build startup/API/normal-SRT/media/Docker-log workflow passed in 7.530 seconds; both containers were up and logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate warning.
- 2026-07-13: Added opt-in wall-time freshness enforcement to `worker-benchmark`. `--max-validation-gap-seconds N` requires at least two validation starts per running stream and fails closed when the conservative cycle-boundary upper bound for any initial, repeat, or trailing gap exceeds `N`; the JSON report includes first-coverage/gap upper bounds and measured duration, while default behavior remains unchanged.
- Live 1,000-URL cadence evidence: exact task-only Linux source booted the simultaneous 500-SRT and 500-DASH fleets and composed exact 500/500 protocol plus 800/100/50/50 behavior counts. One worker completed 250 cycles in 176.355 seconds with eight validation tokens, 30 ms stream budgets, and a 1 ms aggregate budget. All 1,000 URLs received at least two attempts; first coverage was bounded at 125 cycles/87.426 seconds and the worst initial/repeat/trailing gap at 125 cycles/89.651 seconds, passing 125-cycle and 90-second gates. Cycle p50/p95/p99 was 705.693/720.782/736.738 ms; CPU p50/p95/p99 was 685.241/701.701/708.282 ms; worker and child peak RSS were each 77,598,720 bytes; descriptors remained four. Outcomes were 463 success, 50 issue, 1,487 bounded timeout, and 498,000 explicit skips. Fixture logs, resource snapshots, states, report, and hashes were retained under `/tmp/videosim-freshness-evidence`; `capacityCertified=false` remained enforced.
- Validation, startup, and skipped checks: nine focused worker-benchmark/docs tests and the exact task-only full suite passed 378 tests with 84 environment-gated skips. Python compilation, default and production Compose rendering, and diff checks passed. The no-build startup workflow passed the API-ready/worker-register/feed-create/start/normal-SRT-video-audio-captions/stop path in 8.549 seconds; both containers were up, and Docker logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate warning. This is a conservative scheduler-service bound for URLs whose healthy media share one source per protocol, not an approved freshness SLO, per-probe timing, independent-source saturation, failure-domain headroom, HA, or the required 24-hour F5 soak.
- 2026-07-13: Added deterministic 660-SRT, 660-DASH, and mixed-1,320 fixture manifests as a 32%-headroom regression input for the 1,000-stream target. The contract test proves exact 660/660 protocol, 1,056/132/66/66 behavior, and 1,320 distinct-URL composition without adding runtime code.
- Concurrent headroom result: exact task-only Linux source split the live 1,320-URL state across ten concurrent 132-stream workers with 66 SRT and 66 DASH assignments each. A 30 ms pressure pass serviced every URL at least twice within 2.977 seconds but produced 2,377 timeouts. The stronger five-second pass covered every URL at least twice and first coverage stayed within 86.404 seconds, but only eight workers passed the 90-second repeat-gap gate; two measured 91.268 and 91.473 seconds. Aggregate outcomes were 1,056 success, 66 issue, 1,598 timeout, and 87,040 explicit skips. Cycle p95 ranged from 5,084.701 to 5,135.436 ms, CPU p95 from 1,197.920 to 1,327.435 ms, aggregate worker peak RSS was 493,076,480 bytes, and descriptors remained four per worker.
- Resource evidence and skipped gates: during the stronger pass, the SRT fixture reached 3.15 GiB/5,046 processes and threads/606.88% CPU while ten workers used 56.23-90.49 MiB each; SRT memory rose from 2.717 GiB before load to 3.732 GiB afterward. Sixty-two fixture states/logs/stats, worker reports, summaries, and hashes were retained under `/tmp/videosim-headroom-evidence`. This failed single-host saturation run is not actual failure injection/reassignment, 30% post-domain-loss capacity, sufficient healthy-outcome coverage, independent-source load, HA, or a 24-hour F5 soak.
- Validation and startup: 19 focused fixture/docs tests and the exact task-only full suite passed 379 tests with 84 environment-gated skips. Python compilation, default and production Compose rendering, and diff checks passed. The no-build API/normal-SRT/media/Docker-log workflow passed in 8.543 seconds; both containers were up and logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate warning.
- 2026-07-13: Added bounded `validationOutcomesByProtocol` aggregation to `worker-benchmark` JSON and human output using the existing per-stream probe metrics. It retains only protocol/outcome counts, adds no stream IDs to reports, and leaves pass/fail semantics unchanged.
- Protocol calibration: exact task-only Linux source ran two repeated full-media validations against eight distinct healthy SRT and eight distinct healthy DASH URLs. At a five-second stream budget, DASH passed 16/16 while SRT passed its eight fresh connections and timed out all eight reconnects; total measured duration was 5.673 seconds. At a 15-second budget, both protocols passed 16/16, measured duration was 19.859 seconds, and cycle duration was 9.845/9.929/10.013 seconds min/mean/max. Native peer-idle/linger, server-sink, nonblocking, socket-buffer, and leaky-queue experiments did not improve the relay and were discarded. Forty-seven calibration inputs/reports/logs/stats and hashes were retained under `/tmp/videosim-probe-audit-evidence`.
- Validation and startup: nine focused worker-benchmark/docs tests and the exact task-only full suite passed 379 tests with 84 environment-gated skips. Python compilation, default and production Compose rendering, and diff checks passed. The no-build API/normal-SRT/media/Docker-log workflow passed in 8.408 seconds; both containers were up and logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate warning.
- 2026-07-13: Ran the exact committed 1,320-URL mix with measured protocol budgets across 22 concurrent balanced survivor workers. Each worker received 30 SRT/30 DASH streams, 12 validation tokens, a 15-second stream budget, and ten cycles to guarantee two attempts per stream. All 1,320 streams received at least two attempts and first coverage remained at or below 83.991 seconds, but only eight workers passed the 90-second repeated-gap gate; the worst upper bound was 99.052 seconds.
- Calibrated saturation evidence and skipped gates: protocol outcomes were SRT 41 success/124 issue/1,155 timeout and DASH 1,056 success/66 issue/198 timeout. During load, 24 containers used 5.31 GiB and 7,386 processes/threads; workers alone used 3.06 GiB and about 954% aggregate CPU before fixture CPU. Seventy-seven states/reports/logs/stats and hashes were retained under `/tmp/videosim-calibrated-headroom-evidence`. The task-only source was unchanged from commit `51b0244`, whose 379-test/full-startup gate already passed; this documentation-only evidence slice reruns docs contracts and diff checks, while actual domain loss/reassignment, multi-host capacity, independent sources, HA, and 24-hour soak remain skipped.
- 2026-07-13: Added the exact multi-host F5 workload candidate derived from the calibrated survivor run. It places 33 workers evenly across three failure domains with 60 total, 30 SRT, and 30 DASH tokens each; losing any 11-worker domain leaves 22 survivors with exact 1,320/660/660 total/SRT/DASH capacity. The repository F5 contract test loads this manifest and locks that survivor math without treating configuration as evidence.
- Validation: 11 focused scale-evidence/documentation tests and the full task-only suite passed 379 tests with 84 environment-gated skips. Python compilation, default Compose rendering, production Compose rendering with the documented example environment, and diff checks passed; production rendering without required OIDC secrets failed closed as designed.
- Startup and skipped checks: the no-build API workflow passed readiness, worker registration, feed create/start, normal SRT video/audio/captions validation, and stop in 8.491 seconds. Captured Compose state showed app and worker up; Docker logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate negotiation warning. The candidate has not been approved or run across multiple hosts; real domain loss/reassignment, endpoint storms, independent media sources, HA, immutable admission artifacts, and the 24-hour soak remain open.
- 2026-07-13: Added an exact scheduler regression for the checked-in F5 candidate. The capacity-aware durable placement model starts 1,320 streams across 33 workers at 40 each; removing one 11-worker domain leaves 22 workers at exact 60-stream, 30-SRT/30-DASH loads and moves only the unavailable domain's 440 assignments.
- Validation: 12 focused scale-evidence/documentation tests and the full task-only suite passed 380 tests with 84 environment-gated skips. Python compilation, default and production Compose rendering, and diff checks passed. The exact 1,320-stream/33-worker process-local benchmark preserved coverage and rejected 11 stale reports, but reassigned all 1,320 streams, 880 above the minimum, so `capacityCertified=false` remains correct.
- Startup and skipped checks: the no-build API/normal-SRT/media workflow passed in 8.365 seconds. Captured Compose state showed app and worker up; Docker logs had no traceback, fatal error, or validation failure while retaining the known caption-framerate warning. The new regression is an in-memory scheduler model; live PostgreSQL expiry, worker termination/partition, separate hosts, media reassignment, HA, and the 24-hour F5 soak remain untested.
